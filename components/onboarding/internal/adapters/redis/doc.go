package redis

// Package redis provides a minimal consumer-side Redis repository used by the
// onboarding service for cache operations. The implementation wraps the shared
// Redis client and exposes Set/Get/Del primitives.
