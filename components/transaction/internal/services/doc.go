package services

// Package services contains the transaction service use-case layer. It
// orchestrates command and query flows, balances, and routing caches without
// depending on storage implementations.
