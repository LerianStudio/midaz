# Valkey configuration file
bind 0.0.0.0
port 5704
requirepass lerian

# User configuration
# Default user is disabled for security
user default off

# Create user 'midaz' with password 'lerian' and full permissions  
user midaz on >lerian ~* &* +@all