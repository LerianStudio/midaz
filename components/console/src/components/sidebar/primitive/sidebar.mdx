import { Meta, Controls, Primary, Canvas, Stories } from '@storybook/addon-docs/blocks'
import * as Story from './sidebar.stories'

<Meta of={Story} />

# Sidebar

The sidebar of the application, containing a header with logo, all navigation items
and a expand button at the bottom

<Primary />
<Controls />
