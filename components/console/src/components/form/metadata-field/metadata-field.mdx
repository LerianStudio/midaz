import { Meta, Controls, Primary, Canvas, Stories } from '@storybook/addon-docs/blocks'
import * as Story from './metadata-field.stories'

<Meta of={Story} />

# MetadataField

A custom field component, split on a input and data visualization parts, coupled together with react-hook-form.

<Primary />
<Controls />
