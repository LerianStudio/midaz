import { Meta, Controls, Primary, Canvas } from '@storybook/blocks'
import * as Story from './confirmation-dialog.stories'

<Meta of={Story} />

# Confirmation Dialog

Confirmation Dialog is a simple and quick component for when an interaction needs user attention.
Should only have a title, description and two buttons, one cancel and another confirm actions.

<Primary />
<Controls />
