import {
  Meta,
  Controls,
  Primary,
  Canvas,
  Stories
} from '@storybook/addon-docs/blocks'
import * as Story from './stepper.stories'

<Meta of={Story} />

# Stepper

A set of steps that are used to indicate progress through a multi-step process.

<Canvas of={Story.Primary} />

### Controlled

Example below show how they can be controlled to show the correct step sequentially.

<Canvas of={Story.Controlled} />

<Primary />
<Controls />
