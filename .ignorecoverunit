# Patterns to exclude from unit test coverage
# One pattern per line (glob style)
# Lines starting with # are comments

# Infrastructure adapters (tested via integration tests)
*.postgresql.go
*.mongodb.go
*.rabbitmq.go
*.redis.go

# Auto-generated mocks
*_mock.go

# Bootstrap/initialization code (minimal logic)
/bootstrap/
/cmd/app/
bootstrap.go

# Fees Mongo adapters (tested via integration tests). Both dirs hold per-operation
# adapter files (create.go/find.go/update.go/delete.go/indexes.go) that the
# *.mongodb.go glob above does not catch; pack additionally has a package.mongodb.go
# (already caught), so the dir-level entry excludes the whole adapter consistently.
/internal/adapters/mongodb/fees/billing_package/
/internal/adapters/mongodb/fees/pack/

# Auto-generated API docs
/api/

# HTTP routing infrastructure (no business logic)
routes.go
swagger.go
