# Midaz Go SDK Environment Variables

# Authentication token for the Midaz API
MIDAZ_AUTH_TOKEN=your_auth_token_here

# API URLs (defaults to local Docker setup if not specified)
MIDAZ_ONBOARDING_URL=http://localhost:3000
MIDAZ_TRANSACTION_URL=http://localhost:3001

# Debug mode (set to true for verbose output)
MIDAZ_DEBUG=false

# Timeout in seconds for API requests
MIDAZ_TIMEOUT=30
