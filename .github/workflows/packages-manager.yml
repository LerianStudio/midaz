name: "packages manager"

on:
  pull_request:
    branches:
      - develop
      - main
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  get_branch:
    runs-on: ubuntu-24.04
    outputs:
      is_main: ${{ steps.verify_branch.outputs.is_main }}

    steps:
      - uses: actions/checkout@v4
      - id: verify_branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CURRENT_BRANCH=$(gh pr view --json headRefName -q ".headRefName" 2>/dev/null || echo ${{ github.ref_name }})
          if [ "$CURRENT_BRANCH" = "main" ]; then
            echo "is_main=true" >> $GITHUB_ENV
            echo "is_main=true" >> $GITHUB_OUTPUT
          else
            echo "is_main=false" >> $GITHUB_ENV
            echo "is_main=false" >> $GITHUB_OUTPUT
          fi

  check_branch:
    needs: get_branch
    runs-on: ubuntu-24.04
    steps:
      - name: Run to print variable
        run: |
          echo "heeeeeeeeeeeeeeeeeeeeeelp"
          echo "The formatted_release value is: ${{ needs.get_branch.outputs.is_main }}"    

  brew_release:
    needs: get_branch
    runs-on: ubuntu-24.04
    if: needs.get_branch.outputs.is_main == 'false'
    steps:
      - name: Run only if NOT main branch
        run: |
          echo "This step runs only on branches other than main"

  chocolatey_release:
    needs: get_branch
    runs-on: ubuntu-24.04
    if: needs.get_branch.outputs.is_main == 'true'
    steps:
      - name: Run only if NOT main branch
        run: |
          echo "This step runs only on the main branch"