package transaction

// Package transaction provides parsing and validation facilities for the Gold
// transaction DSL. It exposes a visitor that converts parsed trees into the
// internal transaction model and a validator for syntax errors.
