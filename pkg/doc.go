package pkg

// Package pkg provides shared foundation helpers such as error wrappers used
// across services. These helpers compose with core models without depending on
// service-specific behavior.
