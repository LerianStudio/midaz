# F5 — Contract harmonization (task breakdown)

> **Status:** task breakdown of `docs/v4/PLAN.md` §9. The macro plan is authoritative for scope and gates; this file decomposes that scope into atomic, independently-verifiable tasks. Where HEAD has drifted from the macro plan's HEAD-pinned evidence, the task is written against HEAD truth and the drift is recorded in the task Notes and in the drift log returned with this deliverable.
> - **Date:** 2026-06-04
> - **Branch:** `feat/monorepo-consolidation` (Q1-RESOLVED: all v4 work continues here).

F5 collapses the API contract to one source of truth: the swagger served on `:3002` must enumerate exactly the routes the unified ledger binary mounts (onboarding + transaction + holders + instruments + fees + composition), and nothing it does not. The phase fixes the swag-discovery mechanism so CRM-tree annotations fold into the ledger root spec (DC-1), normalizes every spec/env/doc to the single `v4.0.0` train (DC-2/D-11), repairs the broken `make generate-docs` pipeline, regenerates the postman/api artifacts, deletes the orphaned `components/crm/api/` spec set, syncs `llms*.txt`, and lands a CI spec-vs-routes diff gate (DC-3) so served and mounted can never silently diverge again. External docs-portal/auth-server coordination is listed, not executed.

**Hard ordering constraint (R11 / Gate 2).** The swag-fold spike (F5-T01) runs FIRST and BEFORE any downstream doc/postman/version work. If the relocation in DC-1 does not surface `/v1/holders` (post-D-1, `/v1/.../instruments`) in a locally-generated `swagger.json`, D-11's "single spec" premise collapses and the phase re-plans. No task after F5-T01 starts until the spike proves the fold path.

**Upstream-merge precondition.** F5 needs F1+F2+F3+F4 merged (§3 DAG; F3 gates F5 specifically). The Alias→Instrument rename (F1/F2), the holders-accounts and composition routes (F1/F4), the namespace flip (F2), and any tracer HTTP-shape change (F3) are SOURCE changes F5 consumes and regenerates. F5 owns no Go source rename — it runs the generator over already-final source. Every HEAD count below is re-measurable after the upstream phases merge; the gates run at execution time and are authoritative.

---

## 1. Task summary table

| ID | Title | Depends on | Traces to (scope item / exit gate) | Risks mitigated |
|----|-------|-----------|-------------------------------------|-----------------|
| F5-T01 | Swag-fold spike: relocate one CRM handler under ledger `http/in`, run `swag init`, confirm `/v1/.../instruments` appears (DC-1) | — (F1/F2 merged) | DC-1; S5; Gate 2; Risk R11 | R11 |
| F5-T02 | Complete DC-1 relocation: move all CRM `@Router` handlers into the ledger `http/in` tree | F5-T01 | DC-1; S5/S6; Gate 2 | R11, R24 |
| F5-T03 | Fix `generate-docs.sh` `COMPONENTS`: drop the broken `crm` leg → `("ledger")` | F5-T02 | S3/S4; Gate 3; Risk R12 | R12 |
| F5-T04 | Bump ledger swag general-info: `@version v4.0.0`, rewrite `@description` to enumerate all folded domains | F5-T01 | S1; Gate 4 | R13, R44 |
| F5-T05 | Single version source: `VERSION=v4.0.0` in `.env`/`.env.example`, keep `SWAGGER_VERSION=${VERSION}` (DC-2) | — | S9/S10; DC-2; Gate 4 | R44, R13 |
| F5-T06 | Regenerate the ledger `api/` artifact set (`docs.go/swagger.json/swagger.yaml/openapi.yaml`) | F5-T02, F5-T03, F5-T04, F5-T05 | S2; Gate 2, Gate 3 | R13, R31 |
| F5-T07 | Delete the ENTIRE `components/crm/api/` directory (all four orphaned files) | F5-T06, F5-T08 | S7; Gate 4; Risk R-local (orphan re-intro) | R10 |
| F5-T08 | Drop the `CRM_API` leg from `sync-postman.sh`; collapse merge to ledger-only | F5-T06 | S11; Gate 5 | R13 |
| F5-T09 | Retire stale split-service postman env vars (`onboardingPort=3000`/`transactionPort=3001`); prune/gitignore `postman/backups/*` | — | S13; Gate 4, Gate 5 | R50 |
| F5-T10 | Regenerate postman collection + environment via the repaired pipeline; assert `v4.0.0` | F5-T03, F5-T06, F5-T08, F5-T09 | S11/S18; Gate 4, Gate 5 | R13, R44 |
| F5-T11 | Normalize tracer + reporter-manager specs to `4.0.0` train + Q12 titles (`Tracer API`, `Reporter API`); regenerate | — | S12; Gate 6; Q12/Q13 | R44 |
| F5-T12 | Re-point `tests/helpers/env.go` defaults `:3000`/`:3001` → `:3002` (idempotent with F0-T02) | — | S14; Gate 4 | R50 |
| F5-T13 | Sync `llms.txt` + `llms-full.txt`: instruments noun, composition endpoint, removed `/aliases`+`plugin-crm`, v4.0.0 | F5-T02 | S15; Gate 7 | — |
| F5-T14 | Spec-vs-routes diff gate: Go test building the unified app, `app.GetRoutes()` vs served swagger `paths` (DC-3) | F5-T06 | S8/S17; DC-3; Gate 1 | R13 |
| F5-T15 | External-coordination note: docs.lerian.studio, auth-server RBAC (R9/X1), APIDog — listed NOT executed | — | S16; Gate 8; X1/X4/X5 | R1 |

Risk IDs reference `docs/v4/PLAN.md` §11 (consolidated register). §9 phase-local risk numbers map to §11 rows: §9-Critical(plugin-crm)→R1; §9-High(swag fold unverified)→R11; §9-High(generate-docs broken)→R12; §9-Medium(multi-artifact drift)→R13/R31; §9-Medium(orphaned-spec re-intro)→R10-class; §9-Medium(version fork)→R44; the §11 R31 residual (phantom holder-YAML refs) is carried into F5-T06/F5-T14 Notes.

---

## 2. Tasks

### F5-T01 — Swag-fold spike (DC-1, R11): prove CRM annotations fold into the ledger root spec

**Objective.** Before any downstream doc work, empirically prove the DC-1 fold path: relocate ONE annotated CRM handler into the ledger `http/in` tree, run `swag init` from the ledger root, and confirm the holders/instruments path appears in the generated `swagger.json`. This is the gating spike for the whole phase (R11).

**Touchpoints (HEAD, verified).**
- Proven fold path: fees handlers are `package in` under `components/ledger/internal/adapters/http/in/` (`fees_package_handler.go:5` declares `package in`) and ARE discovered by the ledger `swag init`.
- NOT-folded source: CRM handlers are also `package in` but under `components/crm/adapters/http/in/` — `holder.go:5`, `alias.go:5` (post-F2: `instrument.go`). `crmhttp` IS imported by the ledger bootstrap (`components/ledger/internal/bootstrap/config.go:32`), yet the ledger spec carries zero CRM-entity paths — direct evidence that the import graph alone does not drive swag discovery.
- swag invocation: `swag init -g cmd/app/main.go -o api --parseDependency --parseInternal` (`scripts/generate-docs.sh:93`), swag pinned `v1.16.6` (`:227`).
- `@Router` annotations exist on the CRM handlers: 5 in `holder.go`, 6 in `alias.go` (post-F2 the alias set becomes `/v1/.../instruments*`).

**Change shape.** Spike (throwaway-or-promote): copy/relocate one CRM handler file (e.g. the instrument handler after F2 rename) into `components/ledger/internal/adapters/http/in/`, run `cd components/ledger && swag init -g cmd/app/main.go -o api --parseDependency --parseInternal --outputTypes go,json,yaml`, inspect the output for `/v1/holders/{holder_id}/instruments`. Small: one file move + one generator run. If the fold works, F5-T02 promotes it; if it does not, STOP and re-plan (the fees fold shape is the only proven fallback and DC-1 already chose it).

**Verification (narrowest first).**
```bash
# After relocating one handler and running swag init in components/ledger:
jq -r '.paths | keys[]' components/ledger/api/swagger.json | grep -E '/v1/holders/\{holder_id\}/instruments'
```
Must return at least one path. (Pre-F2 sandbox dry-run may use `/v1/holders/{holder_id}/aliases` against the unrenamed source to prove the mechanism alone.)

**Dependencies.** F1+F2 merged (the renamed `instrument.go` source must exist). No intra-F5 dependency — this is the first task.

**Notes.** R11 is the highest structural risk in F5: the fold is mechanically unverified in the planning sandbox. This task de-risks the entire phase before any version/postman work is spent. Do NOT trust `--parseDependency` to walk the `components/crm` subtree — the current run already imports CRM and produces zero CRM paths; relocation under the `--parseInternal` tree is the proven mechanism.

---

### F5-T02 — Complete the DC-1 relocation: all CRM `@Router` handlers under ledger `http/in`

**Objective.** Move the annotated CRM handler files (post-F2: `holder.go` + `instrument.go`) into `components/ledger/internal/adapters/http/in/` so `swag init` discovers every holder/instrument path, mirroring the fees fold.

**Touchpoints (HEAD, verified).**
- Source handlers: `components/crm/adapters/http/in/holder.go` (`HolderHandler` struct at `:22`, 5 `@Router`), `components/crm/adapters/http/in/alias.go` (`AliasHandler` at `:24`, 6 `@Router`; F2 renames to `instrument.go`).
- Destination tree: `components/ledger/internal/adapters/http/in/` (already holds the proven-folded fees handlers; package `in`).
- Both source and destination are `package in` — relocation is package-compatible.
- Route registrar wiring: `RegisterCRMRoutesToApp` (`components/crm/adapters/http/in/routes.go:84`), consumed by bootstrap via `crmhttp.CreateCRMRouteRegistrar` (`config.go:32` import, `config.go:839` per macro plan). The handler structs move; the registrar that references them must follow or import the new location.

**Change shape.** Move handler files (with their `@Router`/`@Tags`/`@Param`/`@Success` annotation blocks intact) into the ledger `http/in` package; update the registrar's references and the bootstrap import (`crmhttp`). Medium: a file relocation plus import-path fix-ups. No annotation TEXT changes here — F2 already renamed the annotations on source; F5 only relocates so swag can see them. Keep the route paths and authz wiring byte-identical (the namespace flip is F2's; F5 must not re-touch it).

**Verification (narrowest first).**
```bash
go build ./components/ledger/... && \
( cd components/ledger && swag init -g cmd/app/main.go -o api --parseDependency --parseInternal --outputTypes go,json,yaml ) && \
jq -r '.paths | keys[]' components/ledger/api/swagger.json | grep -cE '/v1/holders|/v1/.*instruments'
```
The count must be `> 0` and cover the expected holder + instrument paths (CRM source = 11 routes: 5 holders + 6 instrument/sub-resource). Broader: `go build ./...`.

**Dependencies.** F5-T01 (spike must pass). Inter-phase: F1 (holders-accounts route source), F2 (instrument rename source + namespace flip), F4 (composition route source) all merged.

**Notes.** This is the load-bearing fold. Gate 2 asserts the POSITIVE direction (instruments paths present); the `/v1/aliases == 0` checks are regression guards only (they already pass at HEAD because the ledger spec has zero CRM-alias paths). Do not conflate the two.

---

### F5-T03 — Repair `generate-docs.sh`: drop the broken `crm` COMPONENTS leg

**Objective.** Make `make generate-docs` exit 0 by removing the `crm` entry that runs `swag init -g cmd/app/main.go` against the deleted `components/crm/cmd/app/main.go` (R12).

**Touchpoints (HEAD, verified).**
- `scripts/generate-docs.sh:16` — `COMPONENTS=("ledger" "crm")`.
- `:93` — `swag_args=(init -g cmd/app/main.go -o api --parseDependency --parseInternal --outputTypes go,json,yaml)` run with `cwd=components/<component>`.
- The `crm` leg fails because `components/crm/cmd/app/main.go` was deleted (P9-T13, commit `0ec72be9d`); the loop `break`s on first failure (`:236-237`), so the whole target exits non-zero today.
- tracer/reporter handling: they are NOT in this script's `COMPONENTS` (their docs regenerate via their own component Makefiles — see F5-T11); this script drives ledger + postman.

**Change shape.** Edit `COMPONENTS=("ledger" "crm")` → `COMPONENTS=("ledger")`. One-line change. The `generate_openapi_spec`/`generate_openapi_yaml` loops and the postman conversion then run only against the (now-CRM-folded) ledger spec.

**Verification (narrowest first).**
```bash
grep -n 'COMPONENTS=' scripts/generate-docs.sh   # shows ("ledger") only, no "crm"
```
Broader (requires swag + docker + node locally): `make generate-docs` exits 0 and `tmp/` is cleaned.

**Dependencies.** F5-T02 (the ledger spec must already fold CRM before the `crm` leg is dropped, else the regenerated spec loses holders). Inter-phase: none beyond F2 (CRM `cmd/app/main.go` already gone at HEAD).

**Notes.** R12 prerequisite. Strictly ordered after F5-T02: dropping the `crm` leg before the fold lands would yield a ledger spec missing holders (Gate 2 fails). The `sync-postman.sh` `CRM_API` leg is a separate fix (F5-T08).

---

### F5-T04 — Bump ledger swag general-info to v4.0.0 and rewrite the description

**Objective.** Update the hand-edited swag general-info block so the regenerated spec carries `v4.0.0` and a description enumerating every folded domain (onboarding + transaction + holders + instruments + fees + composition).

**Touchpoints (HEAD, verified).**
- `components/ledger/cmd/app/main.go:19-33` — `@title Midaz Ledger API`, `@version v3.7.0` (`:20`), `@description` at `:21` (omits CRM holders/instruments + composition), `@host localhost:3002` (`:27`).

**Change shape.** Edit `// @version v3.7.0` → `// @version v4.0.0`; rewrite the `@description` line to enumerate onboarding + transaction + holders + instruments + fees + composition. Keep `@host localhost:3002`. Small: two annotation lines. The generated `docs.go`/`swagger.json`/`swagger.yaml`/`openapi.yaml` derive from this and are rewritten in F5-T06.

**Verification (narrowest first).**
```bash
grep -nE '@version|@description' components/ledger/cmd/app/main.go
```
`@version v4.0.0` present; description mentions holders + instruments + composition. Broader: confirmed after F5-T06 regen via Gate 4.

**Dependencies.** F5-T01 (fold proven). Inter-phase: none (this is annotation source the regen consumes).

**Notes.** DC-2 single-source: the baked `@version` and the runtime `SWAGGER_VERSION=${VERSION}` (F5-T05) must both resolve to `v4.0.0` so the static-vs-runtime fork (R44) closes structurally.

---

### F5-T05 — Single version source: `VERSION=v4.0.0` (DC-2)

**Objective.** Drive every version string off one interpolated source. Set `VERSION=v4.0.0` and keep `SWAGGER_VERSION=${VERSION}` so the baked spec and the runtime override agree.

**Touchpoints (HEAD, verified).**
- `components/ledger/.env:10` — `VERSION=v3.8.0`.
- `components/ledger/.env.example:10` — `VERSION=v3.8.0`.
- `components/ledger/.env:325` / `.env.example:325` — `SWAGGER_VERSION=${VERSION}` (already interpolated; keep as-is).
- `components/ledger/.env:323`/`:326` — `SWAGGER_TITLE`/`SWAGGER_HOST` commented (no change needed).

**Change shape.** Edit `VERSION=v3.8.0` → `VERSION=v4.0.0` in both `.env` and `.env.example`. Leave `SWAGGER_VERSION=${VERSION}` untouched. Two-line change. This eliminates the baked-`v3.7.0`-vs-runtime-`v3.8.0` drift (R44) by collapsing to one v4.0.0 train.

**Verification (narrowest first).**
```bash
grep -nE '^VERSION=|SWAGGER_VERSION' components/ledger/.env components/ledger/.env.example
```
`VERSION=v4.0.0` in both; `SWAGGER_VERSION=${VERSION}` unchanged.

**Dependencies.** None. Inter-phase: none. Can run in parallel with F5-T04.

**Notes.** F6 (D-11) owns the broader `/v3→/v4` module/doc sweep and the release-tag reconciliation; F5 only fixes the contract-surface version source. The `.env` change here is the v4.0.0 product-train value F6's release pipeline expects.

---

### F5-T06 — Regenerate the ledger `api/` artifact set

**Objective.** Regenerate all four ledger spec files so holders + instruments + composition appear and every version string is `v4.0.0`, in one generator pass.

**Touchpoints (HEAD, verified).**
- `components/ledger/api/{docs.go,swagger.json,swagger.yaml,openapi.yaml}` — at HEAD: `swagger.json:18` version `v3.7.0`, title `Midaz Ledger API` (`:8`), host `localhost:3002` (`:20`); zero `/v1/aliases` paths (`grep -c '/v1/aliases' swagger.json` = 0); the only `alias` paths are the `Account.Alias` routing handle `/v1/.../accounts/alias/{alias}` (DO-NOT-TOUCH, D-1 boundary).
- Regen command: `cd components/ledger && swag init -g cmd/app/main.go -o api --parseDependency --parseInternal --outputTypes go,json,yaml` then the `openapi.yaml` docker step (`generate-docs.sh:120-127`).

**Change shape.** Run `make generate-docs` (after F5-T03 fix) OR the swag+docker steps directly. Regeneration rewrites the entire four-file set. Large generated diff, but mechanical — review for: holder/instrument paths present, `v4.0.0` everywhere, `localhost:3002` host preserved.

**Verification (narrowest first).**
```bash
# Fold completeness (POSITIVE, load-bearing) — path-scoped to avoid prose false-positives:
jq -r '.paths | keys[]' components/ledger/api/swagger.json | grep -cE '/v1/holders|instruments'  # > 0
# Version normalized across the set:
grep -rn 'v3\.7\.0\|v3\.8\.0' components/ledger/api/   # empty
# Regression guard (already passes at HEAD):
grep -c '/v1/aliases' components/ledger/api/swagger.json   # 0
grep -c '/v1/aliases' components/ledger/api/swagger.yaml   # 0
```

**Dependencies.** F5-T02 (fold), F5-T03 (pipeline), F5-T04 (general-info), F5-T05 (VERSION). Inter-phase: F3 (any tracer-driven HTTP shape change must be final), F4 (composition route final).

**Notes (DRIFT — R31).** The macro plan §11 R31 correction holds at HEAD: the ledger `swagger.yaml` carries ZERO holder/CRM-alias path refs today, same as the JSON. Do NOT verify the fold with a bare `grep -c instruments swagger.yaml` — at HEAD that returns 1 from prose ("financial instruments tracked in the ledger", `swagger.json:4052`) and from the `Account.Alias` paths, NOT from a CRM instrument path. The per-artifact-drift argument rests on the VERSION string (`v3.7.0` in all four files, verifiable), not on phantom holder refs. The fold-completeness check above is path-scoped (`jq .paths | keys`) precisely to dodge the prose false-positive.

---

### F5-T07 — Delete the entire orphaned `components/crm/api/` directory

**Objective.** Remove the stale standalone CRM spec set so the only holder/instrument contract is the folded ledger spec. Deleting a subset would leave drift (R10-class) and re-trip the `:4003` grep.

**Touchpoints (HEAD, verified).**
- `components/crm/api/` contains four generated files: `docs.go`, `swagger.json`, `swagger.yaml`, `openapi.yaml`.
- Stale metadata: `docs.go:1599` `Version: "v3.7.0"`, `:1600` `Host: "localhost:4003"`, `:1603` `Title: "CRM API"`; `swagger.json:8` title `CRM API`, `:10` version `v3.7.0`, `:12` host `localhost:4003`.
- This is also the F2 grep-zero carve-out (`components/crm/api/` excluded from F2 Gates 1/2/3 because F5 owns clearing it).

**Change shape.** `git rm -r components/crm/api/`. Deletion. After this, F2's deferred grep-zero (old `@name`/route literals in `docs.go`) is discharged — those strings only survived in the generated artifact F2 could not regenerate.

**Verification (narrowest first).**
```bash
test ! -d components/crm/api/ && echo "deleted"
grep -rn 'localhost:4003' --include='*.json' --include='*.go' --include='*.yaml' components/ docs/ 2>/dev/null  # empty (excluding config_test.go fixtures, see F5-T15/Gate 4)
```

**Dependencies.** F5-T06 (the ledger spec must already fold CRM before the standalone CRM spec is deleted — never delete the only holder doc before the fold lands). F5-T08 (`sync-postman.sh` must stop reading `CRM_API` or it errors on the missing file; order T07 with/after T08's edit).

**Notes.** R10 carve-out closure: deleting the whole directory (all four files) is what makes F2 Gates 2/3 satisfiable end-to-end. Omitting `swagger.yaml` alone re-introduces the `:4003`/`v3.7.0`/`CRM API` drift and re-trips Gate 4.

---

### F5-T08 — Drop the `CRM_API` leg from `sync-postman.sh`

**Objective.** Collapse the postman merge to ledger-only now that holders live in the ledger spec. The CRM conversion leg both points at a soon-deleted spec and double-counts holder paths.

**Touchpoints (HEAD, verified).**
- `scripts/postman-coll-generation/sync-postman.sh:23` — `CRM_API="${MIDAZ_ROOT}/components/crm/api"`.
- `:92-93` — `convert_component "crm" "${CRM_API}/swagger.json"`; `CRM_PID=$!`.
- `:97` — `wait "$LEDGER_PID" "$CRM_PID"`.
- `:102` — `CRM_STATUS=$(...)`.
- `:110` — merge loop `for component in ledger crm`.
- `:178` — hard gate `if [ "$LEDGER_STATUS" != "SUCCESS" ] || [ "$CRM_STATUS" != "SUCCESS" ]`.
- The single-collection path already exists (`:131-134`): with only `ledger`, `num_collections == 1` and the script uses it directly as MIDAZ.

**Change shape.** Remove the `crm` conversion invocation, the `CRM_PID` wait, the `CRM_STATUS` read, the `crm` entry in the merge loop, and drop `CRM_STATUS` from the success gate. Medium: several coordinated deletions in one script. After the edit, only `ledger` converts, and `merge_all_collections` takes the single-collection branch.

**Verification (narrowest first).**
```bash
grep -nE 'CRM_API|CRM_PID|CRM_STATUS|"crm"|for component in ledger crm' scripts/postman-coll-generation/sync-postman.sh  # empty
```
Broader: `make generate-docs` produces a collection containing holder/instrument requests (covered by F5-T10/Gate 5).

**Dependencies.** F5-T06 (holders must be in the ledger spec before the CRM leg is dropped — strict order, Risk: postman missing holders). Pairs with F5-T07 (CRM spec deletion).

**Notes.** Order discipline (§9 Risk "Postman merge masks divergence"): fold CRM into the ledger spec (F5-T06 / Gate 2) BEFORE dropping the `CRM_API` leg.

---

### F5-T09 — Retire stale split-service postman env vars + prune backups

**Objective.** Remove the `:3000`/`:3001` split-service postman environment variables (the unified ledger is `:3002`) and prune or gitignore the 12 postman backup copies that noise-pollute version/port greps.

**Touchpoints (HEAD, verified).**
- `postman/MIDAZ.postman_environment.json:66-67` — `onboardingPort` = `3000`; `:73` `{{baseUrl}}:{{onboardingPort}}`.
- `:108-109` — `transactionPort` = `3001`; `:115` `{{baseUrl}}:{{transactionPort}}`.
- `postman/backups/` — 12 JSON backup files at HEAD.

**Change shape.** Remove the `onboardingPort`/`transactionPort` env entries (and any `{{...Port}}` derived `value` lines) from the environment JSON, or let the regen (F5-T10) drop them — but explicitly assert their absence. Prune `postman/backups/*` or add `postman/backups/` to `.gitignore`. Small.

**Verification (narrowest first).**
```bash
grep -nE '3000|3001' postman/MIDAZ.postman_environment.json   # empty
ls postman/backups/ 2>/dev/null | wc -l                       # 0, OR backups gitignored
git check-ignore postman/backups/ 2>/dev/null                 # matches if gitignored
```

**Dependencies.** Can run in parallel; the env regen (F5-T10) may overwrite the environment file, so the backup prune + the post-regen assertion are the durable parts. Inter-phase: none.

**Notes.** R50: backups are gitignored-or-not at HEAD — verify whether they are tracked before deciding prune vs gitignore. If tracked, `git rm` them; if not, add the gitignore rule so the noise stops recurring.

---

### F5-T10 — Regenerate postman collection + environment; assert v4.0.0

**Objective.** Run the repaired postman pipeline end-to-end so the collection enumerates onboarding + transaction + holders + instruments + fees + composition, carries `v4.0.0`, and drops the `:3000`/`:3001` ports.

**Touchpoints (HEAD, verified).**
- `postman/MIDAZ.postman_collection.json:6` — `"version": "v3.7.0"` (1 hit).
- Pipeline: `convert_to_postman` (`generate-docs.sh:176-194`) → `sync-postman.sh`; collection written to `postman/MIDAZ.postman_collection.json`, env to `postman/MIDAZ.postman_environment.json`.

**Change shape.** Run `make generate-docs` (after F5-T03/T08 fixes) so the collection regenerates from the folded ledger spec. Generated artifact regenerated, not hand-edited. The collection version derives from the ledger spec `v4.0.0`.

**Verification (narrowest first).**
```bash
grep -nE 'v3\.7\.0' postman/MIDAZ.postman_collection.json   # empty
jq -r '.item[].name' postman/MIDAZ.postman_collection.json | grep -iE 'holder|instrument'  # present
```
Broader (Gate 5, requires `make up`): newman run on `:3002` passes for all folded domains.

**Dependencies.** F5-T03 (pipeline), F5-T06 (folded spec), F5-T08 (CRM leg dropped), F5-T09 (port retirement). Inter-phase: none.

**Notes.** Gate 5's newman-against-`make up` is the end-to-end smoke; it is left to execution time (needs a running stack). This task's narrow verification is the static collection content.

---

### F5-T11 — Normalize tracer + reporter-manager specs to the v4.0.0 train (Q12/Q13)

**Objective.** Version/title-normalize the independent tracer and reporter-manager specs to `4.0.0` with the Q12-resolved titles (`Tracer API`, `Reporter API`), then regenerate their full artifact sets. They stay INDEPENDENT specs/ports (Q13) — no `:3002` mega-spec.

**Touchpoints (HEAD, verified).**
- tracer: `components/tracer/api/{docs.go,swagger.json,swagger.yaml,openapi.yaml,types.go}`. `swagger.json:12` version `0.1.0`, `:9` title `Tracer API` (already conforms), `:14` host `localhost:4020`; `docs.go:3124` `Version: "0.1.0"`. `types.go` carries no version string.
- reporter-manager: `components/reporter-manager/api/{docs.go,swagger.json,swagger.yaml,openapi.yaml}`. `swagger.json:8` version `1.2.0`, `:5` title `Reporter` (→ `Reporter API`), `:10` host `localhost:4005`. `reporter-manager/.env:107` `SWAGGER_TITLE='Reporter'` (→ `Reporter API`); `.env:7` `VERSION=v1.0.0` (→ the v4.0.0 train value).
- reporter-worker: no `api/` directory (health-only `:4006`, no REST API — correct, nothing to normalize).
- Regen: each component's own Makefile `swag init` (`mk/docs.mk:39`; `components/tracer/Makefile`); not the root `generate-docs.sh` (which is now ledger-only).

**Change shape.** Update the swag general-info source (the `@title`/`@version` in each component's `cmd/app/main.go`), set `reporter-manager/.env:107` title to `Reporter API` and the `VERSION` env to the v4.0.0 train, then regenerate each component's `api/` set. tracer's `0.1.0 → 4.0.0`; reporter-manager's `1.2.0 → 4.0.0` and `Reporter → Reporter API`. Medium (two components, generated sets). tracer additionally regenerates `types.go`.

**Verification (narrowest first).**
```bash
grep -rn '"version"' components/tracer/api/swagger.json components/reporter-manager/api/swagger.json   # both 4.0.0
grep -rn '"title"' components/reporter-manager/api/swagger.json   # "Reporter API"
grep -rn '"title"' components/tracer/api/swagger.json             # "Tracer API"
```

**Dependencies.** None on F5 tasks; independent of the ledger fold. Inter-phase: F3 (tracer's two-phase reservation endpoints, if their HTTP shape lands HTTP annotations, must be final before tracer regen).

**Notes.** Q13: tracer/reporter keep independent specs/ports/deploy units — F5 only version/title-normalizes; it does NOT merge them into `:3002`. Each documents exactly what it serves (the same served==mounted discipline Gate 1 installs on the ledger). Confirm reporter-manager's `VERSION` env value with the F6 product-train expectation — F6's release version normalization (`v3.7.0`/`0.1.0`/`1.2.0`/`v1.0.0` → `4.0.0`) consumes this.

---

### F5-T12 — Re-point `tests/helpers/env.go` defaults to `:3002`

**Objective.** Bundle the stale split-service test-URL defaults into the F5 sweep so local test runs hit the unified `:3002` ledger.

**Touchpoints (HEAD, verified).**
- `tests/helpers/env.go:27` — `getenv("ONBOARDING_URL", "http://localhost:3000")`.
- `tests/helpers/env.go:28` — `getenv("TRANSACTION_URL", "http://localhost:3001")`.

**Change shape.** Edit both defaults to `http://localhost:3002`. Two-line change.

**Verification (narrowest first).**
```bash
grep -nE 'localhost:300[01]' tests/helpers/env.go   # empty
grep -nE 'localhost:3002' tests/helpers/env.go      # 2 hits
```

**Dependencies.** None. Inter-phase: none.

**Notes (DRIFT / overlap).** This is ALSO F0-T02's fix (F0 Scope (c), §4 Gate 2). At HEAD the file still reads `:3000`/`:3001`, so F0-T02 has NOT landed yet on this branch. The edit is idempotent: whichever phase lands first satisfies both. F5's S14 lists it explicitly; if F0-T02 already merged at F5 execution time, this task is a verified no-op (assert the grep, skip the edit). Do not treat a pre-satisfied state as a failure.

---

### F5-T13 — Sync `llms.txt` and `llms-full.txt` to the v4 contract surface

**Objective.** Update the agent-facing docs to the new surface: instruments noun (D-1), composition endpoint (D-9), removed `/aliases` + `plugin-crm` (D-10), and v4.0.0 language.

**Touchpoints (HEAD, verified).**
- `llms.txt:3` — "CRM holders/aliases ... :3002"; `:5` — "**CRM** (holders/aliases, ... under the `plugin-crm` namespace"; `:25` — "four authz namespaces (`midaz`, `routing`, `plugin-crm`, `plugin-fees`)"; `:39` — "holders and aliases (`plugin-crm`)".
- `llms-full.txt:3` — "CRM holders/aliases"; `:29` — "holder/alias management folded ... (`plugin-crm` namespace"; `:86` — "holder/alias handlers ... (plugin-crm namespace)"; `:294` — "served on `:3002` under the `plugin-crm` authz namespace"; `:300-312` — the alias route table (`/v1/aliases`, `/v1/holders/:holder_id/aliases*`).

**Change shape.** Rewrite the alias/holders prose and the route table to the instrument vocabulary, drop the `/v1/aliases` + `plugin-crm` surface references, add the composition endpoint `POST /v1/holders/:id/accounts`, and align version language to v4.0.0. Medium: prose + one route table. The `plugin-crm` namespace references become the F2-flipped namespace; the four-authz-namespace list updates accordingly.

**Verification (narrowest first).**
```bash
grep -nE '/v1/aliases|plugin-crm' llms.txt llms-full.txt   # empty (post-D-10 surface)
grep -niE 'instrument' llms.txt llms-full.txt              # present
grep -nE '/v1/holders/:id/accounts|composition' llms-full.txt  # present
```

**Dependencies.** F5-T02 (the final route surface must be known). Inter-phase: F1/F2/F4 (the routes the docs describe).

**Notes.** The F2 namespace flip determines the exact replacement namespace string; mirror whatever F2 settled (default candidate per F0 inventory: the ledger's own namespace). Do not invent a namespace name — read it from the merged F2 source.

---

### F5-T14 — Spec-vs-routes diff gate (DC-3)

**Objective.** Land a CI Go test that builds the unified Fiber app, dumps `app.GetRoutes()` into a `METHOD path` set, parses the served/committed swagger `paths`, and asserts set-equality (modulo public infra routes) so served and mounted can never silently diverge again.

**Touchpoints (HEAD, verified).**
- Proven `app.GetRoutes()` pattern: `components/ledger/internal/adapters/http/in/fees_routes_test.go:53` (`for _, r := range app.GetRoutes()`), building a `routeSet` keyed `r.Method+":"+r.Path`. The fee route table at `:21-37` is the canonical shape to extend.
- Committed served spec: `components/ledger/api/swagger.json` (`paths` key set).
- Served instance: `components/ledger/internal/bootstrap/unified-server.go:85-86` mounts `fiberSwagger.InstanceName("swagger")` over the ledger `api.SwaggerInfo`.
- Public infra routes to exclude: `/health`, `/version`, `/readyz`, `/swagger` (registered before auth/tenant middleware — `components/ledger/internal/bootstrap/unified-server.go:82`, CRM `routes.go:45-58`).

**Change shape.** New test file under `components/ledger/internal/adapters/http/in/` (e.g. `contract_spec_routes_test.go`), `TestContractSpecMatchesRoutes`: build the unified app (or its route registrars), collect `app.GetRoutes()`, normalize Fiber `:param` → OpenAPI `{param}`, drop the public-infra set, parse `swagger.json` `paths`, assert set-equality both directions. Medium: one self-contained unit test, no broker/DB. Lock the exclusion list in the test so it cannot silently grow.

**Verification (narrowest first).**
```bash
go test ./components/ledger/internal/adapters/http/in/ -run TestContractSpecMatchesRoutes -count=1
```
Passes: the `app.GetRoutes()` set (normalized, minus infra) equals the committed `swagger.json` `paths`.

**Dependencies.** F5-T06 (the committed spec must be the folded/regenerated one — the test compares against it). Inter-phase: F1/F2/F3/F4 (all mounted routes final).

**Notes.** DC-3 chose the in-process `app.GetRoutes()` comparator over a live `/swagger/doc.json` HTTP probe (the latter needs a running stack, flakier; kept as Gate 5's separate smoke). Normalize path-param syntax (`:id`→`{id}`) and exclude exactly `/health /version /readyz /swagger`. This is the durable anti-drift mechanism — the reason F5 exists at all.

---

### F5-T15 — External-coordination note (listed, not executed)

**Objective.** Produce the F5 coordination note enumerating the out-of-repo items the contract change triggers: docs.lerian.studio publication, the auth-server RBAC policy migration for the flipped `plugin-crm` namespace (R9/X1 class), and the APIDog scenario refresh. F5 lists; it does not execute.

**Touchpoints (HEAD, verified).**
- `plugin-crm` external keying: `components/crm/adapters/http/in/routes.go:21` (`const ApplicationName = "plugin-crm"`); the parallel R9 warning `components/ledger/internal/adapters/http/in/fees_routes.go:16-19` ("tenant-manager RBAC policies key on this string").
- The external coordination register lives in `docs/v4/PLAN.md` §13 (X1 auth-server, X4 APIDog, X5 docs portal); the F0 consumer-coordination inventory (`docs/v4/F0-consumer-coordination-inventory.md`) is the upstream artifact.

**Change shape.** Add a short coordination note (in `docs/v4/` or appended to the F5 execution note) that references the §13 register rows X1/X4/X5 and states explicitly: F5 reflects the new surface in specs/docs; the auth-server policy migration is Fred-owned with the plugin-auth team (X1, a RELEASE/DEPLOY gate, not an F5 merge gate); APIDog and docs portal are owner-driven. Small: a doc note, no code.

**Verification (narrowest first).**
```bash
test -f docs/v4/F5-EXECUTION-NOTE.md && grep -nE 'X1|X4|X5|docs.lerian.studio|auth-server|APIDog' docs/v4/F5-EXECUTION-NOTE.md
```
(Or the standalone coordination note path chosen at execution.) The note names all three out-of-repo items and marks them NOT executed.

**Dependencies.** None hard. Inter-phase: references F0's inventory + §13 register.

**Notes.** R1/X1: the in-code namespace flip is F2's; F5 only regenerates docs reflecting the NEW namespace and LISTS the policy migration. This task is the discharge of Gate 8 — it must not perform the migration.

---

## 3. Gate-closure map

Every exit gate of §9 mapped to the task(s) that close it.

| Exit gate (§9) | Closed by |
|----------------|-----------|
| Gate 1 — Served swagger == mounted routes (diff check clean; `TestContractSpecMatchesRoutes`) | F5-T14 (against the F5-T06 spec) |
| Gate 2 — CRM/instruments folded into the ledger spec in BOTH JSON and YAML (positive fold-completeness load-bearing; `/v1/aliases`==0 regression guards) | F5-T01 (proves mechanism), F5-T02 (folds), F5-T06 (regenerates + asserts) |
| Gate 3 — Pipeline runs end-to-end (`make generate-docs` exits 0; no deleted `crm/cmd/app/main.go` ref; `tmp/` cleaned) | F5-T03 (COMPONENTS fix), F5-T06 (ledger regen), F5-T10 (postman leg) |
| Gate 4 — Zero stale ports/hosts/versions across touched files (`:4003`, `v3.7.0`/`v3.8.0`, `:3000`/`:3001`; tracer/reporter at `4.0.0`) | F5-T04, F5-T05 (ledger version), F5-T06 (ledger regen), F5-T07 (`:4003` deletion), F5-T09 (postman ports), F5-T11 (tracer/reporter), F5-T12 (test URLs) |
| Gate 5 — Postman collection green against `make up` (onboarding+transaction+holders+instruments+fees+composition; stale env entries removed; backups pruned) | F5-T08, F5-T09, F5-T10 |
| Gate 6 — api/ artifacts regenerated for tracer + reporter-manager, full set, `4.0.0` + Q12 titles | F5-T11 |
| Gate 7 — llms sync (instruments noun, composition, removed `/aliases`+`plugin-crm`, v4.0.0; no surviving `/v1/aliases`/`plugin-crm`) | F5-T13 |
| Gate 8 — External coordination listed (docs.lerian.studio, auth-server RBAC R9-class, APIDog) — NOT executed | F5-T15 |

No gate is unmapped. No task exists that fails to trace to a gate.

---

## 4. Risk-to-task map

Every §11 risk owned by F5 (or owned elsewhere but mitigated within F5's tasks) mapped to its carrying task(s).

| Risk (§11) | Sev | Carried by |
|------------|-----|------------|
| R1 — `plugin-crm` flip breaks auth in auth-enabled envs (F5 reflects in docs; X1 is the release gate) | Critical | F5-T13 (docs reflect new namespace), F5-T15 (lists X1 coordination) |
| R11 — swag CRM fold mechanically unverified; if DC-1 fails, the single-spec premise collapses | High | F5-T01 (gating spike, FIRST), F5-T02 (completes fold) |
| R12 — `make generate-docs` broken for crm (deleted `cmd/app/main.go`); breaks the loop | High | F5-T03 (drop the `crm` COMPONENTS leg) |
| R13 — OpenAPI/Postman divergence + version drift masks the real contract | High | F5-T04, F5-T06 (fold + regen), F5-T08/T10 (postman), F5-T14 (diff gate prevents recurrence) |
| R44 — version source fork: baked `v3.7.0` vs runtime `v3.8.0`; D-11 single train | Medium | F5-T04, F5-T05 (single `VERSION=v4.0.0` source, DC-2), F5-T06/T10/T11 (regen to v4.0.0) |
| R31 — RESIDUAL: "ledger swagger.yaml has 4 stale holder refs" is FALSE; rest the drift argument on the version string; positive fold check load-bearing | High | F5-T06 (Notes: path-scoped fold check, version-string drift), F5-T14 (positive served==mounted) |
| R10 — F2 route/`@name` grep-zero unsatisfiable while `components/crm/api/docs.go` exists (F5-owned clear) | High (F2 carve-out, F5 closes) | F5-T07 (delete entire `components/crm/api/`) |
| R50 — stale split-service residue + postman backups noise-pollute greps | Low | F5-T09 (prune/gitignore backups, retire ports), F5-T12 (test URLs) |
| R24 — swagger fold-in unproven for the composition handler (CRM-tree handlers not picked up) | High (F4-owned, F5 verifies) | F5-T01/T02 (the fold path the composition handler also relies on); F4 places its handler in the ledger tree |
| R14 — org-scoped (holders) vs ledger-scoped (accounts) ownership mismatch; F1 decides org-global, F4/F5 honour | High (F1-decided, F5 honours) | F5-T06, F5-T13 (honour: regenerated specs/llms must not assert ledger-locality of holder ownership — describe holders as org-scoped, never ledger-scoped) |

Notes on ownership: R1 and R24 are owned by other phases (F2/auth and F4 respectively); they appear here because F5's tasks carry the contract-surface portion of their mitigation (docs reflection for R1; the fold mechanism for R24). R14 is decided by F1 (org-global ownership matching Mongo storage) and only honoured by F5: its obligation is passive — F5-T06/T13 edits must not introduce a ledger-locality claim for holders. R31 is a §11 residual finding explicitly assigned to F5 in the macro plan — carried in F5-T06 Notes and F5-T14. No F5-owned-or-honoured risk is unmapped.

---

## 5. Execution-notes convention

`docs/v4/F5-EXECUTION-NOTE.md` is created DURING execution (not now): it records decisions made and commit SHAs as tasks land — the swag-fold spike outcome (F5-T01, pass/re-plan), the relocated handler set, the regenerated artifact SHAs, the deleted `components/crm/api/` SHA, and the external-coordination note — mirroring `docs/monorepo/plan/P2a-EXECUTION-NOTE.md`.
