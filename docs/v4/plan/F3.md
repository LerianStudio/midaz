# F3 — Policy seam: tracer inline with two-phase reservation (task breakdown)

> **Status: task breakdown of `docs/v4/PLAN.md` §7.** The macro plan is authoritative for scope and gates; this file decomposes §7 into atomic, independently-verifiable tasks. Where a task touchpoint drifted from the plan's HEAD citation, the task is written against HEAD truth and the drift is recorded in the execution-note convention (§5) and the parent hand-off.
>
> - **Date:** 2026-06-04
> - **Branch:** `feat/monorepo-consolidation`
> - **Baseline SHA verified at authoring:** `2bdbfe556` (matches the PLAN.md F0 epoch baseline).
> - **Phase risk class:** HIGHEST in the major — external dependency on the financial hot path + mutation of the tracer atomic-counter invariant. Double-entry correctness is a third rail (untouched, re-proven by F3-T18).

This phase has two codebases: the **tracer** service (`components/tracer/...`, the reservation engine, schema, reaper, audit) and the **ledger** service (`components/ledger/...`, the `TracerReserver` port/client, settings group, reserve/confirm/release anchors). Tasks are ordered tracer-first (the API contract the ledger calls must exist before the ledger client is wired), with the ledger seam, the proof gates, and docs last.

---

## 1. Task summary table

| ID | Title | Depends on | Traces to (scope / exit gate) | Risks mitigated |
|----|-------|-----------|-------------------------------|-----------------|
| F3-T01 | Tracer reservation DDL: `reserved_usage` column + `usage_reservations` table + indexes (migration 000018/000019) | — | Scope "Usage counter schema"; design call B; Gate 2 | R3, R6, R35 (idempotency index grain) |
| F3-T02 | Reservation domain model + status enum + sentinels/entity constants | F3-T01 | Scope "Audit chain" (event types); design call B | R6, R36 |
| F3-T03 | Atomic reserve CTE: modify WHERE guard to `current_usage + reserved_usage + amount <= maxAmount` | F3-T01 | Scope "Atomic counter CTE" (THE critical correctness line); design call B; Gate 2 | R3 |
| F3-T04 | `usage_reservations` repository: insert-reserve, confirm (reserved→current), release/expire (idempotent) | F3-T01, F3-T03 | Scope "Validation orchestration"; design call B; Gates 1,2 | R3, R4, R6, R35, R38 |
| F3-T05 | Reserve service method: resolve limits once, reserve atomically, return handle | F3-T03, F3-T04 | Scope "Validation orchestration", "Limit check"; design call A | R3, R38 |
| F3-T06 | Confirm + release service methods (own tx, reference resolved limit set, idempotent) | F3-T04, F3-T05 | Scope "Validation orchestration"; design call A; Gate 1 | R4, R21, R38 |
| F3-T07 | Audit events for RESERVED/CONFIRMED/RELEASED/SKIPPED (per-row) + EXPIRED batch-summary | F3-T02, F3-T05, F3-T06 | Scope "Audit chain"; Q11-resolved | R19 |
| F3-T08 | `POST /v1/reservations` + `/:id/confirm` + `/:id/release` handlers, validation, routes, swagger | F3-T05, F3-T06 | Scope "Tracer write API"; design call A; Gates 4,5 | R6 |
| F3-T09 | Reservation reaper worker (sub-minute cadence, own knob, `EnsureWorkers` per-tenant) | F3-T04, F3-T07 | Scope "TTL reaper"; Gate 7 | R4, R21, R22 |
| F3-T10 | `tracer` ledger settings group (`mode`/`failPosture`/`timeoutMs`) with explicit enum-membership validation | — | Scope "Per-ledger settings"; design call C; Gate 4/5 wiring | R36, R37 |
| F3-T11 | `TracerReserver` port + HTTP client (fetcher `CircuitBreakerExecutor` pattern, NOT the RabbitMQ wrapper) + M2M + timeout | F3-T08, F3-T10 | Scope "Ledger→tracer client"; design call "Ledger→tracer client" | R20, R21 |
| F3-T12 | Inject `TracerReserver` on `TransactionHandler` at the composition root | F3-T11 | Scope "Handler DI" | — |
| F3-T13 | Reserve anchor: call reserve immediately before `ProcessBalanceOperations` (`transaction_create.go:1228`), fail-posture branch | F3-T11, F3-T12 | Scope "Reserve anchor"; design calls D,E,C; Gates 4,5 | R3, R17, R20 |
| F3-T14 | Confirm/release transport at the post-commit lib-streaming emit slot (`send_transaction_events.go:173/238`) | F3-T13 | Scope "Confirm anchor"; design calls E,G | R21 |
| F3-T15 | PENDING lifecycle: reserve at create-pending, confirm at commit, release at cancel; long-lived TTL flag | F3-T13, F3-T14 | Scope "Commit/cancel (pending)"; design call F | R18 |
| F3-T16 | Revert no-refund verification: assert revert touches no reservations | F3-T13, F3-T14 | Scope "Revert"; Q9-resolved | — |
| F3-T17 | PROOF: crash-convergence interleavings + WHERE-guard over-commit concurrency test | F3-T04, F3-T09, F3-T13, F3-T14 | Gates 1, 2 | R3, R4 |
| F3-T18 | PROOF: double-entry untouched (PD-5 re-proof) with reserve seam active | F3-T13 | Gate 6; third rail | — |
| F3-T19 | PROOF: fail-open SKIPPED + fail-closed reject behavior tests | F3-T13, F3-T07 | Gates 4, 5 | R20 |
| F3-T20 | PROOF: k6 latency budget + audit-lock contention report | F3-T13, F3-T14, F3-T07 | Gate 3 | R19 |
| F3-T21 | Reaper cadence proof: sub-minute release, skip-on-pool-failure, never touches root pool | F3-T09 | Gate 7 | R22, R4 |
| F3-T22 | Tracer swagger annotations for reservation endpoints + version-train hand-off note | F3-T08 | Scope "Docs" | — |

---

## 2. Tasks

### F3-T01 — Tracer reservation DDL: `reserved_usage` column + `usage_reservations` table

**Objective.** Add the hybrid storage (Q10-resolved): a `reserved_usage` bucket on `usage_counters` for the fast in-tx guard, plus a `usage_reservations` table for per-reservation TTL, idempotent confirm/release, and audit correlation.

**Touchpoints (verified at HEAD).**
- `components/tracer/migrations/` — latest is `000017_audit_actor_in_hash.up.sql`; new files are `000018_add_reserved_usage_column.{up,down}.sql` and `000019_create_usage_reservations.{up,down}.sql`.
- `components/tracer/migrations/000004_initial_schema.up.sql:114-122` — current `usage_counters` DDL: single `current_usage BIGINT`, `CONSTRAINT usage_counters_composite_unique UNIQUE (limit_id, scope_key, period_key)`. NO `reserved_usage` exists at HEAD.
- `components/tracer/migrations/000011_add_counter_expires_at.up.sql` — exemplar for a single-purpose ALTER on `usage_counters` (`expires_at` already exists per the plan; reservation TTL is per-row in the new table, NOT this column).
- Tracer migrations apply via the tracer migrator (`make migrate`, golang-migrate); single-tenant or per-tenant DB depending on MT.

**Change shape.** Two new migration pairs. `000018` runs `ALTER TABLE usage_counters ADD COLUMN reserved_usage BIGINT NOT NULL DEFAULT 0 CHECK (reserved_usage >= 0);` (down drops it). `000019` creates `usage_reservations` per the PLAN.md §7 design-call-B DDL sketch: PK `id UUID`, `limit_id UUID NOT NULL REFERENCES limits(id) ON DELETE CASCADE`, `scope_key VARCHAR(255)`, `period_key VARCHAR(50)`, `amount BIGINT CHECK (amount >= 0)`, `status VARCHAR(16) NOT NULL DEFAULT 'RESERVED'`, `transaction_id UUID` (correlation, NOT a FK — cross-service), `reservation_expires_at TIMESTAMPTZ NOT NULL`, `created_at/confirmed_at/released_at`. Two indexes: the idempotency unique index `idx_usage_reservations_request ON usage_reservations(transaction_id, limit_id, scope_key, period_key)` and the reaper partial index `idx_usage_reservations_reaper ON usage_reservations(reservation_expires_at) WHERE status = 'RESERVED'`. The idempotency index grain MUST be the full 4-tuple (R35: a single ledger txn touching one limit across two scopes/periods yields two reservation rows; `(transaction_id, limit_id)` alone collides). Add a `CHECK (status IN ('RESERVED','CONFIRMED','RELEASED','EXPIRED'))` to keep the enum honest at the DB.

**Verification.** Narrowest: `make migrate && make migrate-down && make migrate` from `components/tracer` exits 0 (round-trips). Then `psql ... -c '\d usage_reservations'` shows the table + both indexes and `\d usage_counters` shows `reserved_usage`. Broader: `make migrate-lint` (if present in tracer) or the tracer migration linter; the tracer integration suite boots against testcontainers with the new schema.

**Dependencies.** None (DAG root of the phase). Inter-phase: none hard — F3 runs parallel to F1/F2.

**Notes.** No `-- no-transaction` directive: per F1's onboarding precedent and the absence of any such directive in tracer migrations, do NOT add one. The `usage_reservations.status` enum is a CHECK + Go-side enum (F3-T02), not a PG enum type, to avoid the `ALTER TYPE ... ADD VALUE` migration friction the tracer already hit in `000009`.

### F3-T02 — Reservation domain model + status enum + sentinels/entity constants

**Objective.** Give the reservation a domain entity, a status enum, error sentinels (TRC-XXXX), and audit event-type constants so service/repo/handler code references typed values, not literals.

**Touchpoints (verified at HEAD).**
- `components/tracer/pkg/model/` — domain entities live here (e.g. `validation.go`, `limit.go`). New `reservation.go`.
- `components/tracer/pkg/constant/errors.go` (21.6K, TRC-XXXX codes) — sentinel home. `TRC-0220..0249` is already the allocated "Validation Request Errors" block (`:161`; `TRC-0220..0237` in use, mapped in `validation_handler.go:182`), `TRC-0250..0269` is "Audit / Transaction Validation Errors", and `TRC-0270..0279` is in use. New reservation sentinels take a FRESH, unallocated TRC block (e.g. `TRC-0280+`) — do NOT continue from `0220`, which would collide with the validation range.
- `components/tracer/pkg/model/audit_event.go` (per CLAUDE.md model inventory) — audit event-type constants; add RESERVED/CONFIRMED/RELEASED/EXPIRED/SKIPPED.

**Change shape.** New `model/reservation.go`: `Reservation` struct mirroring the DDL columns, a `ReservationStatus` string enum (`StatusReserved`/`StatusConfirmed`/`StatusReleased`/`StatusExpired`), a `NewReservation(...)` constructor validating invariants (amount ≥ 0, non-empty scope/period, non-nil limit ID) returning `(*Reservation, error)` per tracer's constructor convention, and a `Validate()` method. Amounts use `int64` (the counter column is BIGINT; tracer stores smallest currency unit) — NOT `decimal` here, consistent with `current_usage BIGINT`. Add new sentinels in `errors.go` (e.g. `ErrReservationNotFound`, `ErrReservationAlreadyTerminal`, `ErrReservationAmountInvalid`) with fresh TRC codes. Add the five audit event-type constants alongside the existing validation/limit event types.

**Verification.** Narrowest: `go build ./components/tracer/pkg/model/... ./components/tracer/pkg/constant/...`. Then `go test ./components/tracer/pkg/model/ -run TestReservation` (table-driven constructor invariants, using `testutil.MustDeterministicUUID` / `testutil.FixedTime`, never `uuid.New()`/`time.Now()` per tracer CLAUDE.md).

**Dependencies.** F3-T01 (column/table semantics drive the struct). 

**Notes.** SKIPPED is an audit event type but never a `usage_reservations.status` value (no row is written when the tracer is skipped fail-open) — keep the DB CHECK to the four persisted states and SKIPPED as an audit-only constant.

### F3-T03 — Atomic reserve CTE: modify the WHERE guard (THE critical correctness line)

**Objective.** Change the over-limit guard so it accounts for outstanding reservations: `current_usage + reserved_usage + amount <= maxAmount`. Getting this wrong reintroduces the exact TOCTOU over-limit bug the current CTE prevents (R3).

**Touchpoints (verified at HEAD).**
- `components/tracer/internal/adapters/postgres/usage_counter_repository.go:49-72` — `upsertAndIncrementCTEQuery`. Current WHERE guard at `:58`: `WHERE usage_counters.current_usage + $9 <= $10`. ON CONFLICT grain `(limit_id, scope_key, period_key)` at `:53`. The `current_usage` increment is at `:55`.

**Change shape.** Add a reserve-path CTE (either a new `upsertAndReserveCTEQuery` const or a parameterized variant) whose UPDATE increments `reserved_usage` (not `current_usage`) and whose WHERE guard is `WHERE usage_counters.current_usage + usage_counters.reserved_usage + $amount <= $maxAmount`. The INSERT branch seeds `reserved_usage` with the amount and `current_usage` 0. Keep the existing `upsertAndIncrementCTEQuery` for the legacy synchronous `POST /v1/validations` path (design call A keeps it intact). Use `squirrel` only where the existing repo does; this CTE is a hand-written const string parameterized positionally, consistent with HEAD — do NOT rewrite it into squirrel (matches the existing repo style and the SQL-construction rule's "after ToSql" caveat does not apply to a static CTE).

**Verification.** Narrowest grep gate (Gate 2 partial): `grep -n "reserved_usage" components/tracer/internal/adapters/postgres/usage_counter_repository.go` shows the new guard line. Then `go test ./components/tracer/internal/adapters/postgres/ -run TestUsageCounter` (sqlmock unit asserting the emitted SQL contains `current_usage + usage_counters.reserved_usage + ` in the WHERE). The full concurrency proof is F3-T17.

**Dependencies.** F3-T01 (`reserved_usage` must exist).

**Notes.** This is the single most review-sensitive line in the phase. The task is intentionally narrow (one CTE) so the diff is reviewable in isolation; the correctness proof lives in F3-T17, not here.

### F3-T04 — `usage_reservations` repository: reserve/confirm/release/expire

**Objective.** Repository over `usage_reservations`: insert a reservation row (idempotent on the 4-tuple), confirm (flip `reserved_usage`→`current_usage` + mark row CONFIRMED), release/expire (decrement `reserved_usage` + mark row RELEASED/EXPIRED). Each lifecycle op is one atomic tx touching both the counter row and the reservation row.

**Touchpoints (verified at HEAD).**
- New `components/tracer/internal/adapters/postgres/usage_reservation_repository.go`, mirroring `usage_counter_repository.go` structure (`conn pgdb.Connection`, `NewUsageReservationRepositoryWithConnection`).
- `components/tracer/internal/adapters/postgres/db/` — `DB`/`Connection`/`TxBeginner` interfaces (reuse).
- Repository interface defined in the consuming package: per CLAUDE.md "interfaces defined where used", declare `ReservationRepository` in `internal/services/query` (reserve/read) and/or `internal/services/command` following the existing `UsageCounterRepository` split.

**Change shape.** New repo with `Reserve(ctx, *model.Reservation) error` (INSERT ... ON CONFLICT (transaction_id, limit_id, scope_key, period_key) DO NOTHING for idempotency, paired with the reserve CTE from F3-T03 in one tx), `Confirm(ctx, reservationID) error` (UPDATE counter `current_usage += amount, reserved_usage -= amount` + UPDATE reservation `status='CONFIRMED', confirmed_at=now()` guarded `WHERE status='RESERVED'` so a retried confirm is a no-op), `Release(ctx, reservationID, status)` (UPDATE counter `reserved_usage -= amount` + reservation `status=RELEASED|EXPIRED` guarded `WHERE status='RESERVED'`). All three are `BeginTx`-wrapped. squirrel with `PlaceholderFormat(squirrel.Dollar)` for the UPDATE/INSERT statements; check `RowsAffected()` to map zero rows to the already-terminal sentinel (idempotent no-op) vs not-found.

**Verification.** Narrowest: `go test ./components/tracer/internal/adapters/postgres/ -run TestUsageReservation` (sqlmock unit: assert reserve uses the 4-tuple ON CONFLICT, confirm's `WHERE status='RESERVED'` guard, release decrements `reserved_usage`). Broader: integration test against testcontainers asserting confirm is idempotent (second confirm = 0 rows affected, no double-move). Repo/adapter code is integration-tested with real PG per CLAUDE.md SQL rules.

**Dependencies.** F3-T01, F3-T03.

**Notes.** The confirm "move" and the release "decrement" MUST be in the same tx as the reservation-row state flip — a partial apply is exactly the divergence the TTL reaper would otherwise have to clean up. The reaper (F3-T09) calls `Release(..., EXPIRED)`.

### F3-T05 — Reserve service method

**Objective.** Service entry that resolves applicable limits once, computes the reservation amount per limit/scope/period, and reserves atomically, returning a reservation handle (id) to the caller. This is the ALLOW-path persistence in the two-phase model.

**Touchpoints (verified at HEAD).**
- `components/tracer/internal/services/validation_service.go:166-377` — `Validate` is the existing synchronous orchestration; single-tx boundary documented at `:156-165`. The reserve method is a NEW sibling method (or a new `reservation_service.go`), NOT a rewrite of `Validate` (design call A: `/v1/validations` stays intact).
- `components/tracer/internal/services/query/limit_checker.go` — limit resolution: `checkLimitsInternal` at `:147`, `getApplicableLimits` at `:499-538` (limits read from DB per request, NOT cached). [DRIFT: plan §7 cited the limit-check span at `:341-495` and the upsert call at `:432-441`; at HEAD the orchestration entry is `checkLimitsInternal:147` and `getApplicableLimits:499`. Written against HEAD.]

**Change shape.** New `Reserve(ctx, req)` service method: resolve limits via `getApplicableLimits` once, for each applicable limit build a `model.Reservation`, call the repo `Reserve` paired with the reserve CTE (F3-T03/T04). Carry the resolved limit set on the returned handle / reservation rows so confirm/release need NOT re-query (R38). Decision precedence stays as the existing `Validate` documents; a reserve that fails the WHERE guard returns the limit-exceeded decision exactly as the synchronous path does today. Start a span `service.reservation.reserve`, `logging.WithTrace`.

**Verification.** Narrowest: `go test ./components/tracer/internal/services/ -run TestReserve` (gomock the repo + limit reader; assert single limit-resolution call, reservation built per applicable limit). 

**Dependencies.** F3-T03, F3-T04.

**Notes.** Reserve dedup carries the ledger `transactionID` (the 4-tuple index) so a retried reserve does not double-reserve (R11/R35).

### F3-T06 — Confirm + release service methods

**Objective.** Service methods that confirm or release a reservation, each in its own tx, idempotent, referencing the resolved limit set from the reservation rows (no second limit lookup).

**Touchpoints (verified at HEAD).**
- `components/tracer/internal/services/validation_service.go` / new `reservation_service.go` — sibling to the reserve method (F3-T05).
- The repo `Confirm`/`Release` from F3-T04.

**Change shape.** `Confirm(ctx, reservationID)` and `Release(ctx, reservationID)` service methods, each opening its own tx via the repo, each emitting the corresponding audit event (F3-T07). A confirm/release against an already-terminal row is a no-op (the repo `WHERE status='RESERVED'` guard returns 0 rows; the service maps that to success, not error — idempotent retry). Spans `service.reservation.confirm` / `.release`.

**Verification.** Narrowest: `go test ./components/tracer/internal/services/ -run 'TestConfirm|TestRelease'` (gomock repo: assert idempotency — second confirm returns nil without a second counter move).

**Dependencies.** F3-T04, F3-T05.

**Notes.** Confirm/release do NOT re-resolve limits (R38) — the reservation row carries `limit_id`/`scope_key`/`period_key`/`amount`.

### F3-T07 — Audit events: per-row transitions + batch-summary expiry

**Objective.** Emit audit events for real reserve/confirm/release transitions per-row (full forensic trail) and a single batch-summary audit event for reaper-driven EXPIRED sweeps (Q11-resolved: cap hash-chain contention on the high-volume/low-forensic-value path). SKIPPED is audited per-row at the ledger-fail-open decision.

**Touchpoints (verified at HEAD).**
- `components/tracer/internal/services/command/record_audit_event.go` — `AuditWriter` implementation. At HEAD the interface declares `RecordValidationEvent` (`:100`), `RecordRuleEventWithTx` (`:169`), and `RecordLimitEventWithTx` (`:207`) — the Rule/Limit recorders carry the `WithTx` suffix so the audit row persists atomically with the entity mutation (the CLAUDE.md interface table is stale on this point; HEAD is authoritative). Add reservation event recording following the `*WithTx` pattern.
- `components/tracer/migrations/000017_audit_actor_in_hash.up.sql:61` — `pg_advisory_xact_lock(314159265)` per audit insert (the single global hash-chain lock; contention is R19).
- Audit event-type constants from F3-T02.

**Change shape.** Add `RecordReservationEventWithTx(ctx, db, ...)` (or extend the writer) so reserve/confirm/release each write one audit row carrying the reservation id + transition — `WithTx` so the audit row commits in the SAME tx as the counter move / state flip (mirroring `RecordRuleEventWithTx`/`RecordLimitEventWithTx`, consistent with the same-tx invariant in the Notes below). SKIPPED is the ledger fail-open decision (no counter move), so it is recorded via the non-tx `RecordReservationEvent` surface. Add a `RecordReservationExpiryBatch(ctx, summary)` that writes ONE audit row summarizing a reaper sweep (count, period window) rather than one-per-expired-row. Both go through the existing hash-chain advisory-lock path (unchanged lock semantics).

**Verification.** Narrowest: `go test ./components/tracer/internal/services/command/ -run TestRecordReservationEvent` (gomock: per-transition writes one row; batch writes exactly one row for N expiries). 

**Dependencies.** F3-T02, F3-T05, F3-T06 (transitions to audit), F3-T09 references the batch path.

**Notes.** Per-tenant DB scopes the advisory-lock contention per-tenant (R19); the latency/contention measurement is F3-T20.

### F3-T08 — Tracer `/v1/reservations` handlers, validation, routes, swagger

**Objective.** Expose the two-phase API as a new resource (design call A): `POST /v1/reservations`, `POST /v1/reservations/:id/confirm`, `POST /v1/reservations/:id/release`. Leave `POST /v1/validations` untouched.

**Touchpoints (verified at HEAD).**
- `components/tracer/internal/adapters/http/in/routes.go:294-335` — rules/limits/validations/audit route block. `POST /v1/validations` at `:329` via `guard.With("validations", "post", cfg.APIKeyOnlyValidation)`. New reservation routes register in the same `api` group with a new `reservations` resource guard.
- `components/tracer/internal/adapters/http/in/validation_handler.go:47` (`NewValidationHandler`), `:81` (`Validate`), `@Router /v1/validations [post]` at `:80` — the handler shape to mirror. New `reservation_handler.go`.
- `components/tracer/internal/adapters/http/in/*_validation.go` — per-resource input validation pattern; new `reservation_validation.go`.

**Change shape.** New `reservation_handler.go` with three Fiber handlers (extract ctx, span, parse, call service F3-T05/T06, respond via `libHTTP.Created`/`libHTTP.OK` — never `c.JSON`). Reserve request body mirrors the validation request shape but returns a reservation handle; confirm/release take the `:id` UUID path param. Routes added after `:329` under `guard.With("reservations", "post", ...)`. Swagger annotations (`@Router /v1/reservations [post]` etc.) on each handler; `reservation_validation.go` for input validation. Cursor pagination is N/A (no list endpoint introduced).

**Verification.** Narrowest: `go build ./components/tracer/internal/adapters/http/in/...` then `go test ./components/tracer/internal/adapters/http/in/ -run TestReservationHandler` (Fiber app test asserting 201 on reserve, 200 on confirm/release, 4xx on bad input). Route presence: a `app.GetRoutes()`-style test asserting the three paths are mounted (the tracer has route-table tests in this package).

**Dependencies.** F3-T05, F3-T06.

**Notes.** The reservation resource is a NEW authz resource string `"reservations"` in the tracer's guard — this is the tracer's own API-key/Access-Manager guard, NOT the ledger `plugin-*` namespace (that is F2's concern; unrelated here).

### F3-T09 — Reservation reaper worker (sub-minute, own knob, per-tenant)

**Objective.** A background worker that releases expired RESERVED rows at a sub-minute cadence, per-tenant via `EnsureWorkers`, with its own enable/interval knob distinct from the 24h cleanup worker.

**Touchpoints (verified at HEAD).**
- `components/tracer/internal/services/workers/usage_cleanup_worker.go:25-36` — `UsageCleanupWorkerConfig`/`DefaultUsageCleanupWorkerConfig` (24h default, ticker pattern) — the structural template for the new reaper.
- `components/tracer/internal/services/workers/supervisor.go:279-338` — `WorkerSupervisor.EnsureWorkers` (per-tenant spawn, cap check at `:323`, `context.Background()` worker context, skip-on-failure semantics). The reaper joins the same per-tenant spawn set.
- Tracer config env (tracer CLAUDE.md "Cleanup Worker": `CLEANUP_WORKER_ENABLED`/`CLEANUP_INTERVAL_HOURS`) — add a NEW `RESERVATION_REAPER_ENABLED` + `RESERVATION_REAPER_INTERVAL_SECONDS` (sub-minute default, e.g. 30s), NOT reusing `CLEANUP_INTERVAL_HOURS`.

**Change shape.** New `reservation_reaper_worker.go` modeled on `usage_cleanup_worker.go`: a per-tenant ticker that queries `usage_reservations WHERE status='RESERVED' AND reservation_expires_at < now()` (uses the `idx_usage_reservations_reaper` partial index) and calls repo `Release(..., EXPIRED)` for each, then writes one batch-summary audit event (F3-T07). Wired into `EnsureWorkers` alongside the cleanup worker. The worker MUST skip its cycle (not touch the root pool) when the tenant pool fails to resolve, mirroring the cleanup worker's pool-resolution guard.

**Verification.** Narrowest: `go test ./components/tracer/internal/services/workers/ -run TestReservationReaper` (fake clock + gomock repo: expired rows get Release; non-expired untouched; cadence honored). Integration proof of "released within interval" + "never touches root pool" is F3-T21.

**Dependencies.** F3-T04 (Release), F3-T07 (batch audit).

**Notes.** R22: a tenant over the worker cap (default 100) never spawns a reaper. The cap-independent-sweep concern is the carried OQ; for F3 the reaper follows the `EnsureWorkers` cap-respecting pattern with skip-on-failure and the over-cap case is documented as a known limitation in the execution note, not silently ignored.

### F3-T10 — `tracer` ledger settings group with enum-membership validation

**Objective.** Add a top-level `tracer` group to `LedgerSettings` — `tracer.mode` (off|advisory|enforce, default off), `tracer.failPosture` (open|closed, default open), `tracer.timeoutMs` (number, default 250) — and add explicit allowed-value validation because the existing validator checks type only, not membership (design call C; R36).

**Touchpoints (verified at HEAD).**
- `pkg/mmodel/settings.go:27-30` (`LedgerSettings` struct, currently only `Accounting`), `:32-44` (`AccountingValidation`), `:48-51` (`defaultAccountingValidation`), `:55-59` (`DefaultLedgerSettings`), `:64-71` (`DefaultLedgerSettingsMap`), `:75-82` (`LedgerSettingsToMap`).
- `pkg/mmodel/settings.go:133-138` (`settingsSchema` — add a `"tracer"` group), `:143-149` (`knownNestedFieldNames` auto-derived).
- `pkg/mmodel/settings.go:164-222` (`ValidateSettings`) and `:227-247` (`validateSettingsFieldType` — checks ONLY primitive type: bool/string/number; NO enum membership). The enum-membership check is the new code.
- `pkg/mmodel/settings_test.go` — extend.

**Change shape.** Add a `Tracer` struct (`Mode string`, `FailPosture string`, `TimeoutMs int`) to `LedgerSettings`; add `"tracer"` to `settingsSchema` mapping `mode`/`failPosture` → `"string"`, `timeoutMs` → `"number"`; extend both default maps and `LedgerSettingsToMap`; add a defaults var. In `ValidateSettings`, after the existing type check, add explicit membership validation for `tracer.mode ∈ {off,advisory,enforce}` and `tracer.failPosture ∈ {open,closed}` returning a business error (reuse `ErrInvalidSettingsFieldType` or a new membership sentinel) — so `"enfroce"` is rejected at write time rather than silently defaulting. Regenerate the `@Description` prose and swagger via `make generate-docs` (ledger).

**Verification.** Narrowest: `go test ./pkg/mmodel/ -run TestValidateSettings` (cases: valid enum accepted, `"enfroce"`/`"closd"` rejected, type-correct-but-out-of-range rejected, defaults applied on miss). Then `go build ./pkg/mmodel/...`.

**Dependencies.** None hard. Soft inter-phase: F1 (D-4 `RequireHolder`) establishes the settings-group pattern under `accounting`; F3 adds a top-level `tracer` group on the same machinery. If F1 has not merged, F3 lands the enum-validation hook itself (the plan notes the dependency is "soft / interface contract", not a hard blocker).

**Notes.** Read on the hot path is via the cached `GetParsedLedgerSettings` (already used at `transaction_create.go:1134`), NOT the uncached `GetSettings`. The 5-min cache TTL means posture flips lag (R37) — documented, accepted for advisory→enforce.

### F3-T11 — `TracerReserver` port + HTTP client

**Objective.** A `TracerReserver` port on the ledger side and an HTTP client implementing it over the fetcher `CircuitBreakerExecutor` pattern (timeout + breaker + M2M JWT), NOT the RabbitMQ-coupled midaz `CircuitBreakerManager` wrapper.

**Touchpoints (verified at HEAD).**
- `pkg/reporter/fetcher/client.go:25-44` (per-op timeout constants), `:75-87` (`FetcherClient` struct + `CircuitBreakerExecutor` interface), `:95-106` (functional options `WithM2MTokenProvider`/`WithCircuitBreaker`), `:132` (`NewFetcherClient`) — the precedent pattern to mirror.
- The midaz `CircuitBreakerManager` wrapper is RabbitMQ-coupled (constructor requires non-nil `*libRabbitmq.RabbitMQConnection`, registers only `rabbitmq.CircuitBreakerServiceName`, RabbitMQ health checker) — do NOT use as-is. Either stand up a second breaker registration following the fetcher executor, or generalize the wrapper beyond RabbitMQ; the fetcher executor is the anchor.
- Port location: define `TracerReserver` interface in the package that owns the contract — the `command`/`http in` seam consuming it (mirror how `FeeApplier` is declared near `TransactionHandler`, `transaction.go:26-29`).

**Change shape.** New client package (e.g. `components/ledger/internal/adapters/tracer/client.go`) with a `TracerClient` struct holding `baseURL`, `httpClient`, `m2mProvider`, `cbExecutor`, and per-op timeout from `tracer.timeoutMs`. Methods `Reserve(ctx, req) (handle, error)`, `Confirm(ctx, id) error`, `Release(ctx, id) error` calling the F3-T08 endpoints. Define the `TracerReserver` interface (the narrow port the handler depends on) with the same three methods. A nil reserver means "disabled" (mode=off), guarded at the call site like the streaming nil-emitter pattern. On timeout/breaker-open the client returns a typed error the anchor (F3-T13) maps via `failPosture`.

**Verification.** Narrowest: `go build ./components/ledger/internal/adapters/tracer/...` then `go test ./components/ledger/internal/adapters/tracer/ -run TestTracerClient` (httptest server: timeout path returns the breaker/timeout error; 200 path parses the handle; M2M header attached when provider set).

**Dependencies.** F3-T08 (the endpoints it calls), F3-T10 (`timeoutMs`).

**Notes.** Do NOT depend on a concrete client in handler code — the handler depends on the `TracerReserver` interface (mirrors the streaming `Emitter`-interface rule and the `FeeApplier` precedent).

### F3-T12 — Inject `TracerReserver` at the composition root

**Objective.** Wire the tracer client onto `TransactionHandler` at the same composition-root line that injects `FeeApplier`.

**Touchpoints (verified at HEAD).**
- `components/ledger/internal/adapters/http/in/transaction.go:23` (`type TransactionHandler struct`), `:26-29` (`FeeApplier FeeApplier` field + doc) — add a `TracerReserver TracerReserver` field.
- `components/ledger/internal/bootstrap/config.go:798` — `transactionHandler := &httpin.TransactionHandler{Command: commandUseCase, Query: queryUseCase, FeeApplier: fees.useCase}` — append `TracerReserver: <built tracer client or nil>`.
- Tracer client construction in `config.go` near the fee/streaming wiring; gated by config (build a noop/nil when tracer integration disabled, mirroring `STREAMING_ENABLED=false` → `NewNoopEmitter`).

**Change shape.** Add the struct field + the bootstrap construction (read tracer base URL / M2M from config, build the F3-T11 client, inject). When tracer mode is globally off / unconfigured, inject nil and the anchor short-circuits.

**Verification.** Narrowest: `go build ./components/ledger/...`. Then `go vet ./components/ledger/internal/bootstrap/...`. Behavioral wiring is exercised by F3-T13's tests.

**Dependencies.** F3-T11.

**Notes.** No behavior change here — pure DI. The per-ledger `tracer.mode` gate (not a global flag) lives at the call site (F3-T13); the bootstrap nil is only the "tracer integration entirely absent" escape hatch.

### F3-T13 — Reserve anchor before `ProcessBalanceOperations` + fail-posture branch

**Objective.** Call reserve on FEE-INCLUSIVE amounts immediately before the balance commit, and branch on `tracer.failPosture` when the tracer is unavailable.

**Touchpoints (verified at HEAD).**
- `components/ledger/internal/adapters/http/in/transaction_create.go:1228` — `result, err := handler.Command.ProcessBalanceOperations(...)`. This is the true balance-commit boundary, synchronous in both sync/async modes. The reserve call goes immediately BEFORE this (design call D).
- `transaction_create.go:1075` (`applyFees`), `:1106` (post-fee re-validation), `:1134` (`GetParsedLedgerSettings`) — the reserve MUST sit downstream of `applyFees` (fee-inclusive) and can read the already-fetched `ledgerSettings` from `:1134` (no second settings read). [Confirmed: `applyFees:1075`, re-validate:1106, settings:1134, ProcessBalanceOperations:1228 — all match the plan.]
- `tracer.mode`/`failPosture`/`timeoutMs` from the `ledgerSettings.Tracer` group (F3-T10).

**Change shape.** Between `:1227` and `:1228`, when `ledgerSettings.Tracer.Mode == "enforce"` (advisory = call-but-do-not-block; off = skip) and the reserver is non-nil, call `handler.TracerReserver.Reserve(ctx, ...)` with the fee-inclusive validate/balanceOps amounts and the `transactionID`. On reserve-denied (limit exceeded): reject (delete idempotency key, remove from Redis queue, return business error) BEFORE `ProcessBalanceOperations`. On timeout/breaker-open: `failPosture==open` → record SKIPPED audit (via the reserver/client surface) + proceed; `failPosture==closed` → reject (no balance mutation, idempotency key released). Store the returned reservation handle(s) for the confirm/release transport (F3-T14). The reserve seam must NOT mutate `Send.Value` or any balance math (third rail).

**Verification.** Narrowest: `go build ./components/ledger/internal/adapters/http/in/...` then `go test ./components/ledger/internal/adapters/http/in/ -run TestTransactionCreateReserveAnchor` (stub `TracerReserver`: enforce+allow proceeds; enforce+deny rejects before commit; off/advisory skip the block). Fail-open/closed behavior is F3-T19; the structural placement is asserted here.

**Dependencies.** F3-T11, F3-T12.

**Notes.** Design call E: in async mode the reserve still fires here at HTTP time (`RABBITMQ_TRANSACTION_ASYNC` defers only Postgres row persistence, not the synchronous balance commit at `:1228`). The legacy `Version==""` consumer-path `UpdateBalances` carve-out (transient pre-v3.6 messages) is outside the reserve/confirm seam — do not anchor there.

### F3-T14 — Confirm/release transport at the post-commit emit slot

**Objective.** Fire confirm (success) / release (abort) from the post-commit lib-streaming lifecycle-event slot, through the tracer client. Best-effort transport; the TTL reaper is the durability backstop (design call G).

**Touchpoints (verified at HEAD).**
- `components/ledger/internal/services/command/send_transaction_events.go:173` (`emitTransactionLifecycleEvent`), `:238` (`pkgStreaming.EmitImportant(ctx, span, logger, uc.Streaming, definitionKey, buildFn)`) — the post-commit emit slot. [Confirmed: the `EmitImportant` call at `:238` has NO separate `source` arg — source is owned by the producer/Builder, matching the F1 §11 note. Confirm transport co-locates here, NOT in `SendTransactionEvents:68`, which is the legacy RabbitMQ `ProducerDefault:114` path.]

**Change shape.** Co-locate a `confirm`/`release` call alongside the lifecycle emit (post balance-commit). On the success phase, call `TracerReserver.Confirm(ctx, handle)`; on abort/error paths that already remove the transaction, call `Release`. Failures here MUST NOT fail the request — log Warn + span-record + continue (mirrors the IMPORTANT-posture streaming convention); the reaper releases any lost confirm/release at TTL. The reservation handle from F3-T13 is threaded through to this slot (carry it on the command input / context the lifecycle emit already receives).

**Verification.** Narrowest: `go test ./components/ledger/internal/services/command/ -run TestConfirmReleaseTransport` (stub reserver: success phase → Confirm; abort → Release; transport error → Warn-logged, request still succeeds). 

**Dependencies.** F3-T13.

**Notes.** Do not build the outbox (out of scope). The whole point of the TTL (D-6 mandatory) is that this transport can be lost and counters still converge.

### F3-T15 — PENDING lifecycle wiring + long-lived TTL flag

**Objective.** PENDING transactions reserve at create-pending, confirm at commit, release at cancel; pending reservations get a long-lived TTL distinct from direct-transaction TTL (design call F, option i).

**Touchpoints (verified at HEAD).**
- `components/ledger/internal/adapters/http/in/transaction_create.go:1130` — `if transactionStatus != constant.PENDING` branch; PENDING creation flows through `executeCreateTransaction`. The reserve at create-pending rides the same anchor (F3-T13) but tags the reservation long-lived.
- `components/ledger/internal/adapters/http/in/transaction_state_handlers.go:366` (`commitOrCancelTransaction`), `:525` (its `ProcessBalanceOperations`) — confirm at commit (status APPROVED), release at cancel (status CANCELED). [Confirmed: `commitOrCancelTransaction:366`, `ProcessBalanceOperations:525`; `CommitTransaction:49`→`:84`, `CancelTransaction:107`→`:142`.]
- `transaction_state_handlers.go:378` — the 300s Redis lock is mutual-exclusion, NOT a reservation TTL (do not conflate).

**Change shape.** Pass a `longLived bool` (or a TTL override) into the reserve request when `transactionStatus == PENDING` so `reservation_expires_at` is set far out (or effectively unbounded) — distinct from the sub-minute direct-transaction TTL. In `commitOrCancelTransaction`, after the `ProcessBalanceOperations` at `:525` (APPROVED) call `Confirm`; on CANCELED call `Release`. Same non-blocking, log-Warn-on-failure posture as F3-T14.

**Verification.** Narrowest: `go test ./components/ledger/internal/adapters/http/in/ -run TestPendingReservationLifecycle` (stub reserver: create-pending reserves long-lived; commit confirms; cancel releases). 

**Dependencies.** F3-T13, F3-T14.

**Notes.** PENDING rows persist indefinitely with no existing sweep (R18) — a short TTL would expire reservations for still-valid pendings. The long-lived flag is the chosen divergence; the reaper's partial index still covers them but they will not expire under normal pending lifetimes.

### F3-T16 — Revert no-refund verification

**Objective.** Assert (Q9-resolved) that reverting a transaction does NOT touch reservations — limits measure gross activity; a revert is itself a new chargeable transaction.

**Touchpoints (verified at HEAD).**
- `components/ledger/internal/adapters/http/in/transaction_state_handlers.go:166` (`RevertTransaction`), `:287` (`createRevertTransaction`) → `executeCreateTransaction(isRevert=true)`. [Confirmed at HEAD.]
- The reserve anchor (F3-T13) runs for the revert's OWN new transaction (the revert is a transaction), but there is NO refund/release of the original transaction's reservation.

**Change shape.** This is primarily a verification/guard task, not new feature code: ensure the revert path does not call `Release`/`Confirm` against the ORIGINAL transaction's reservation. If F3-T13/T14 are implemented correctly, the revert simply creates a new transaction that reserves on its own; the task adds a test that locks this behavior so a future change cannot silently add a refund.

**Verification.** Narrowest: `go test ./components/ledger/internal/adapters/http/in/ -run TestRevertNoReservationRefund` (stub reserver: revert of a confirmed transaction issues a new reserve for the reverse transaction and NEVER calls Release/Confirm against the original reservation id). Grep guard: `grep -rn "Release\|Confirm" components/ledger/internal/adapters/http/in/transaction_state_handlers.go` shows no original-reservation release in the revert branch.

**Dependencies.** F3-T13, F3-T14.

**Notes.** Per-limit gross|net configurability is a v4.1 candidate, explicitly out of scope.

### F3-T17 — PROOF: crash-convergence + WHERE-guard over-commit

**Objective.** The two correctness proofs (Gates 1 and 2): counters converge under crash at every reserve↔confirm interleaving, and the modified WHERE guard never over-commits under concurrency.

**Touchpoints (verified at HEAD).**
- Tracer integration suite home: `components/tracer/tests/integration/` (testcontainers, `-tags integration`, `-p 1`). New `*_test.go` for `TestReservationCrashConvergence` and the over-commit concurrency test.
- The crash interleavings the plan enumerates: (a) after reserve, before balance commit; (b) after balance commit, before confirm transport; (c) after confirm transport, before reaper.

**Change shape.** Two integration tests. Crash-convergence: drive reserve, simulate ledger death at each interleaving (no confirm sent / confirm lost), advance time past TTL or run the reaper, assert `reserved_usage` returns to pre-reserve for aborted transactions and `current_usage` equals exactly the sum of committed amounts. Over-commit: fire N parallel reserves against a limit with capacity N-1; assert exactly N-1 succeed, the Nth is denied, and `current_usage + reserved_usage` never exceeds `maxAmount`.

**Verification.** `go test -tags integration -run TestReservationCrashConvergence ./components/tracer/...` passes for all three interleavings; `go test -tags integration -run TestReservationOverCommit ./components/tracer/...` passes. Plus the Gate 2 grep: `grep -n "reserved_usage" components/tracer/internal/adapters/postgres/usage_counter_repository.go`.

**Dependencies.** F3-T04, F3-T09 (reaper convergence), F3-T13, F3-T14.

**Notes.** This is the phase's load-bearing proof. A property test on the CTE (randomized amounts/capacities) strengthens the WHERE-guard assertion beyond the fixed N-1 case.

### F3-T18 — PROOF: double-entry untouched (PD-5 re-proof)

**Objective.** The sum==0 invariant and the fee-seam structural gate still pass with the reserve seam active in `enforce` mode (Gate 6; third rail).

**Touchpoints (verified at HEAD).**
- `pkg/mtransaction/validations_test.go` (sum-zero invariant tests).
- The fee-seam structural gate `transaction_fee_seam_structure_test.go` (referenced at `transaction_create.go:1053-1054` — the single-`:=`/single-`=` validate seam the gate enforces; the reserve seam must not disturb it).
- `ProcessBalanceOperations` balance math and `ValidateSendSourceAndDistribute` — must show zero diff in balance logic.

**Change shape.** No production change beyond what F3-T13 already added. The task is the re-proof: confirm the existing invariant suites pass unchanged with the reserve anchor present, and `git diff` shows no edit to balance math or the validate-seam shape.

**Verification.** `make test-unit` (includes `pkg/mtransaction/validations_test.go`) green; `make test-integration` green with reserve active in enforce mode; `git diff --stat` shows no change to `ProcessBalanceOperations`/`ValidateSendSourceAndDistribute` balance logic; the fee-seam structural test passes.

**Dependencies.** F3-T13.

**Notes.** If the structural fee-seam gate trips, the reserve anchor was placed inside the protected validate seam — move it strictly between `:1227` and `:1228`, outside the seam.

### F3-T19 — PROOF: fail-open SKIPPED + fail-closed reject

**Objective.** Behavior tests for the two fail postures (Gates 4 and 5).

**Touchpoints (verified at HEAD).**
- The anchor branch in F3-T13; the SKIPPED audit path in F3-T07; the `tracer.failPosture` setting in F3-T10.

**Change shape.** Two tests. Fail-open: `failPosture=open`, tracer client forced to timeout (breaker open) → transaction COMMITS and a SKIPPED audit row is recorded. Fail-closed: `failPosture=closed`, client forced to timeout → transaction REJECTED, no balance mutation, idempotency key released.

**Verification.** `go test -run TestTracerFailOpenSkipped ./components/ledger/...` and `go test -run TestTracerFailClosedReject ./components/ledger/...` pass.

**Dependencies.** F3-T13, F3-T07.

**Notes.** Fail-open is the DEFAULT (D-7) so a degraded tracer cannot block all transactions; fail-closed is the regulated-tenant opt-in.

### F3-T20 — PROOF: k6 latency budget + audit-lock contention

**Objective.** Quantify p99 added latency from reserve+confirm with `tracer.mode=enforce` and the 3x audit-advisory-lock contention vs 1x today (Gate 3).

**Touchpoints (verified at HEAD).**
- k6 load harness against the ledger transaction-create path (the dev-team k6 skill / existing load setup). Limits NOT cached (`getApplicableLimits` reads DB per request) — measure that worst case.
- Audit advisory lock `pg_advisory_xact_lock(314159265)` (`000017:61`): two-phase writes up to 3 audit rows/txn (RESERVED + CONFIRMED + the transaction's own) vs 1 today.

**Change shape.** A k6 script + a documented run producing a latency report artifact (p50/p95/p99 added latency) and the audit-lock contention metric. No production code change.

**Verification.** Load-test report artifact committed under the phase's test/docs area showing p99 within the declared budget and the lock-acquisition count per transaction. Reviewer reads the artifact.

**Dependencies.** F3-T13, F3-T14, F3-T07.

**Notes.** The budget figure is set at execution time; the artifact records it. Per-tenant DB scopes the lock contention per-tenant (R19).

### F3-T21 — PROOF: reaper cadence + isolation

**Objective.** The reservation reaper releases an expired RESERVED row within its sub-minute interval, runs per-tenant via `EnsureWorkers`, and never touches the root pool when a tenant pool fails to resolve (Gate 7).

**Touchpoints (verified at HEAD).**
- `reservation_reaper_worker.go` (F3-T09); `supervisor.go:279-338` (`EnsureWorkers`, cap + skip-on-failure).

**Change shape.** Integration test: seed an expired RESERVED row, run the reaper, assert release within the interval and a batch-summary audit row written; inject a tenant-pool-resolution failure and assert the reaper skips the cycle and never queries the root pool.

**Verification.** `go test -tags integration -run TestReservationReaperCadence ./components/tracer/...` passes (release-within-interval; skip-on-pool-failure; root-pool untouched).

**Dependencies.** F3-T09.

**Notes.** R22 over-cap-tenant limitation is documented in the execution note, not closed by this test (it asserts the cap-respecting path works, not the over-cap sweep).

### F3-T22 — Tracer swagger annotations + version-train hand-off

**Objective.** Annotate the new reservation endpoints and record the tracer version bump as an F5/F6 hand-off (the actual `0.1.0 → 4.0.0` normalization is F5/D-11, not F3).

**Touchpoints (verified at HEAD).**
- Tracer swagger generated via `make generate-docs` → `swag init` (`components/tracer/Makefile`, `mk/docs.mk`); the tracer spec version normalizes from `0.1.0` (in the `swagger.json`/`openapi.yaml` metadata, e.g. `swagger.json:12`), while `components/tracer/api/docs.go:14` carries a templated `"version": "{{.Version}}"`.
- The `@Router`/`@Success`/`@Param` annotations added on the F3-T08 handlers.

**Change shape.** Ensure the F3-T08 handlers carry complete swag annotations; run `make generate-docs` in the tracer component to regenerate `api/*`. Do NOT bump the tracer version string in F3 (F5 owns the version train) — record it as a hand-off line in the execution note.

**Verification.** Narrowest: `cd components/tracer && make generate-docs` exits 0; `grep -c "/v1/reservations" components/tracer/api/swagger.json` > 0. 

**Dependencies.** F3-T08.

**Notes.** Tracer/reporter keep INDEPENDENT specs/ports (Q13-resolved); F3 only adds the reservation paths to the tracer's own spec.

---

## 3. Gate-closure map

Every exit gate from PLAN.md §7 mapped to its closing task(s). A gate with no closing task is a defect.

| Exit gate (PLAN.md §7) | Closing task(s) |
|------------------------|-----------------|
| Gate 1 — Usage-drift crash proof (all three interleavings) | F3-T17 (with F3-T04 repo, F3-T09 reaper, F3-T13/T14 anchors as the SUT) |
| Gate 2 — WHERE-guard over-commit proof + `reserved_usage` grep | F3-T03 (the guard), F3-T17 (concurrency proof + grep) |
| Gate 3 — Latency budget + audit-lock contention report | F3-T20 |
| Gate 4 — Fail-open SKIPPED | F3-T19 (with F3-T13 branch, F3-T07 SKIPPED audit, F3-T10 posture) |
| Gate 5 — Fail-closed reject | F3-T19 (with F3-T13 branch, F3-T10 posture) |
| Gate 6 — Double-entry untouched (PD-5 re-proof) | F3-T18 |
| Gate 7 — Reaper cadence proof (sub-minute, per-tenant, skip-on-pool-failure) | F3-T09 (worker), F3-T21 (proof) |

Supporting scope items not phrased as numbered gates but required by §7, mapped for completeness: API shape (design call A) → F3-T08; hybrid schema (design call B) → F3-T01; settings group + enum validation (design call C) → F3-T10; reserve anchor (design call D/E) → F3-T13; PENDING lifecycle (design call F) → F3-T15; confirm/release transport (design call G) → F3-T14; ledger→tracer client → F3-T11/T12; audit per-row+batch (Q11) → F3-T07; revert no-refund (Q9) → F3-T16; tracer docs → F3-T22.

---

## 4. Risk-to-task map

Every PLAN.md §11 risk OWNED by F3 mapped to the task(s) carrying its mitigation. An unmapped owned risk is a defect.

| Risk (PLAN.md §11, owning phase = F3) | Carrying task(s) |
|---------------------------------------|------------------|
| R2 (Critical, co-owned with F0) — existing `POST /v1/validations` count-commit path treated as greenfield; double-counting risk during the reserve/confirm transition | F3-T03 (legacy upsert CTE left intact; reservation guard is additive), F3-T08 (legacy endpoint untouched — reservations land as a NEW `/v1/reservations` resource per design call A) |
| R3 (Critical) — WHERE-guard regression / TOCTOU over-limit | F3-T03 (the guard line), F3-T17 (proof + property test) |
| R4 (Critical) — crash between reserve and confirm leaks `reserved_usage` | F3-T04 (atomic ops), F3-T09 (reaper), F3-T17 (crash convergence), F3-T21 (cadence) |
| R17 (High) — balance commit synchronous in BOTH modes; mis-placed confirm anchor | F3-T13 (reserve before `:1228` synchronously), F3-T14 (confirm at post-commit slot) |
| R18 (High) — PENDING reservations outlive direct-transaction TTL | F3-T15 (long-lived TTL flag) |
| R19 (High) — audit hash-chain advisory lock contention (3 rows/txn) | F3-T07 (per-row + batch-summary), F3-T20 (contention measurement) |
| R20 (High) — fail-closed tracer timeout blocks all transactions | F3-T11 (timeout + breaker), F3-T13 (fail-open default branch), F3-T19 (proof) |
| R21 (High) — no outbox; confirm/release transport best-effort, can be lost | F3-T06 (idempotent confirm/release), F3-T09 (TTL reaper backstop), F3-T14 (non-blocking transport) |
| R22 (High) — reaper over worker-cap tenant never reaped | F3-T09 (`EnsureWorkers` + documented limitation), F3-T21 (skip-on-failure proof) |
| R35 (Medium) — idempotency-key grain mismatch (2 rows per limit across scopes) | F3-T01 (4-tuple unique index) |
| R36 (Medium) — enum settings pass `ValidateSettings` on bad values (type-only check) | F3-T10 (explicit allowed-value validation) |
| R37 (Medium) — settings 5-min cache TTL lag on posture flip | F3-T10 (documented; cached `GetParsedLedgerSettings` read) |
| R38 (Medium) — limits NOT cached; confirm/release repeats limit List query | F3-T05 (resolve once, carry on reservation rows), F3-T06 (no re-resolve) |

R5 (release-version decoupling) and R45/R46 are F6-owned. No F3-owned §11 risk is unmapped.

---

## 5. Execution-notes convention

`F3-EXECUTION-NOTE.md` is created DURING execution (not now): it captures decisions made and commit SHAs as tasks land, mirroring `docs/monorepo/plan/P2a-EXECUTION-NOTE.md`. Record at minimum: the chosen sub-minute reaper interval default and TTL values (direct vs long-lived pending), the breaker-wiring choice (second registration vs generalized wrapper), the measured k6 latency budget figure (F3-T20), the over-cap-tenant reaper limitation acknowledgement (R22), and the line-drift corrections found vs PLAN.md §7 (limit_checker entry `:147`/`:499` vs the cited `:341-495`; `EmitImportant` signature has no `source` arg).
