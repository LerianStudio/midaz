# F2 — Instruments: rename + CRM contract hard cut (task breakdown)

> **Status:** task breakdown of `docs/v4/PLAN.md` §6. The macro plan is authoritative for scope and gates; this file decomposes that scope into atomic, independently-verifiable tasks. Where HEAD drifted from the macro plan's cited touchpoints, the task is written against HEAD truth and the drift is flagged in the task Notes (and in the structured `notes` output of this breakdown).
> - **Date:** 2026-06-04
> - **Branch:** `feat/monorepo-consolidation`
> - **Module at HEAD:** `github.com/LerianStudio/midaz/v3` (the `/v4` bump is F6; F2 touches only its own import paths if and when the module flips, per D-11).

This phase implements D-1 (Alias→Instrument hard rename), D-10 (full in-code hard cut: old `/aliases*` routes removed AND the `plugin-crm` namespace flipped), and the F2-local tail of D-11. Q3/Q4/Q5/Q7/Q8 are RESOLVED (Fred 2026-06-04) and binding. The external auth-server policy migration is X1 (Fred-owned, release/deploy gate), NOT a merge gate for F2.

---

## 1. Task summary table

| ID | Title | Depends on | Traces to (scope / exit gate) | Risks mitigated |
|----|-------|-----------|-------------------------------|-----------------|
| F2-T01 | DO-NOT-TOUCH allowlist + guard checklist (executable) | — | §6 Risk 3 disambiguation; gate 1/2/3 scoping | R3, R43 |
| F2-T02 | Q7 dual-use audit of `QueryHeader.Alias`; freeze the instrument-domain rename field list | — | §6 Cross-component sweep (Q7); gate 9 | R3 (phase Risk 11) |
| F2-T03 | Namespace-target proposal (resource `instruments`, related-parties sub-resource) | — | §6 (d) Authz; Design call 2 (Q3/Q8) | R1 |
| F2-T04 | Rename `pkg/mmodel/alias.go` → `instrument.go`; rename the type + input + `@name` set | F2-T01 | §6 (a) Go rename inventory; gate 1 | R3 |
| F2-T05 | Rename Mongo adapter package `alias/` → `instrument/` (model, repo, query, maintenance, mock) | F2-T04 | §6 alias-domain filename inventory; gate 1, gate 5 | R4 |
| F2-T06 | Rename CRM service files + `AliasRepo` field + `delete-related-party.go` span attr | F2-T04, F2-T05 | §6 filename inventory (8 services incl. related-party); gate 1 | R4 |
| F2-T07 | Rename CRM handler `alias.go` → `instrument.go`; handler type + methods + swag annotation text | F2-T04, F2-T06 | §6 (e) swagger annotation handling; gate 1 | R4, phase Risk 8/9 |
| F2-T08 | Add `constant.EntityInstrument` + `constant.EntityRelatedParty` (+ `EntityHolder`) | — | §6 entity-name derivation (Q5); gate 4 | R43, phase Risk 7 |
| F2-T09 | Replace the 14 `reflect.TypeOf` sites with the new constants | F2-T04, F2-T08 | §6 entity-name derivation (14 sites); gate 4 | R43, phase Risk 7 |
| F2-T10 | Rename CRM-prefixed error sentinel `ErrHolderHasAliases` (+ ref) and the alias error names | F2-T04 | §6 error codes/sentinels; D-1 ErrHolderHasAliases tail | R43 |
| F2-T11 | Routes hard cut: `/aliases*` → `/instruments*`, no dual-serve | F2-T07, F2-T03 | §6 (c) routes hard cut (D-10); gate 2, gate 8 | R1, R3 |
| F2-T12 | Authz flip: `plugin-crm` namespace + `"aliases"` resource literal removed in code | F2-T03, F2-T11 | §6 (d) Authz; Design call 2 (Q3); gate 2 | R1 |
| F2-T13 | `QueryHeader` instrument-domain filter-field rename (audit-scoped) | F2-T02 | §6 Cross-component sweep (Q7); gate 9 | R3 (phase Risk 11) |
| F2-T14 | Update real router-registration integration tests to assert `/instruments*` | F2-T11 | §6 gate 8 (real router pins) | R1 |
| F2-T15 | Update fixture-stub + rename-test bookkeeping (`routes_test.go`, error/entity tests) | F2-T11, F2-T09 | §6 gate 8 (fixture stubs) | — |
| F2-T16 | Reporter zero-diff confirmation (collection name unchanged) | F2-T05 | §6 (b) Mongo strategy (Q4); gate 7 | R42 |
| F2-T17 | Docs/postman sweep: instruments noun, namespace flip, X1 note | F2-T11, F2-T12 | §6 cross-component + docs sweep; gate 10 | R1, R43 |
| F2-T18 | Full-suite gate: `make test-unit`, `make test-integration`, `make lint`, grep-zero | all | §6 gates 1-10 | R3, R10, R42 |

---

## 2. Tasks

### F2-T01 — DO-NOT-TOUCH allowlist + guard checklist (executable)

**Objective.** Freeze the surfaces a naive `alias`/`aliases` sweep would corrupt, as an executable guard the later rename tasks and the final gate re-run. The collision is `mmodel.Alias` (the renamed entity) vs `Account.Alias` (the routing handle, DO-NOT-TOUCH per D-1).

**Touchpoints (HEAD, verified).**
- Numeric-coded sentinels (must NOT change), `pkg/constant/errors.go`: `ErrAliasUnavailability` 0020 (`:34`), `ErrFailedToRetrieveAccountsByAliases` 0063 (`:77`), `ErrAccountAliasNotFound` 0085 (`:99`), `ErrAccountAliasInvalid` 0096 (`:110`), `ErrAccountingAliasValidationFailed` 0118 (`:132`), `ErrDuplicatedAliasKeyValue` 0123 (`:137`).
- Routing-handle code: `components/ledger/internal/services/fees/midaz/account_resolver.go:85,88,116,137` (`target.Aliases`, `resolveByAliases`, `GetAccountByAlias`).
- Struct fields: `Account.Alias`, `Balance.Alias` (streaming `Account.Alias`/`Balance.Alias`), and the transaction-DSL `@sender`-style handle.
- The shared `QueryHeader.Alias *string` at `pkg/net/http/httputils.go:70` — its fate is decided by F2-T02 (it is the routing-handle account-listing param, so it stays).

**Change shape.** No production edit. Add (or extend) a checklist block to `docs/v4/plan/F2-EXECUTION-NOTE.md` and a small allow-list grep script the gate task reuses. The deliverable is a verifiable set of "these strings must survive F2 unchanged".

**Verification.** Narrowest: `grep -nE 'ErrAliasUnavailability|ErrFailedToRetrieveAccountsByAliases|ErrAccountAliasNotFound|ErrAccountAliasInvalid|ErrAccountingAliasValidationFailed|ErrDuplicatedAliasKeyValue' pkg/constant/errors.go` returns the 6 lines unchanged. `grep -nE 'resolveByAliases|GetAccountByAlias' components/ledger/internal/services/fees/midaz/account_resolver.go` returns the routing-handle sites unchanged.

**Dependencies.** None. Inter-phase: independent of F1/F3.

**Notes.** This is the standing reference the F2-T18 gate greps against. It is the safeguard behind Risk 3 — Gate 1/2/3 are entity-scoped (`mmodel.Alias`), never word-scoped (`alias`).

---

### F2-T02 — Q7 dual-use audit of `QueryHeader.Alias`; freeze the instrument-domain rename field list

**Objective.** Resolve the mandatory Q7 pre-task: determine whether the shared `QueryHeader.Alias` field is dual-use with the ledger account-listing `?alias=` param (in which case it stays) and fix the exact instrument-domain field list F2-T13 renames.

**Touchpoints (HEAD, verified).**
- `pkg/net/http/httputils.go:52-58,70`: instrument-domain candidates `BankingDetailsBranch/Account/Iban` (`:52-54`), `RegulatoryFieldsParticipantDocument` (`:56`), `RelatedPartyDocument`/`RelatedPartyRole` (`:57-58`); the shared `Alias *string` (`:70`).
- `Alias` reader: `components/ledger/internal/adapters/postgres/account/account.postgresql.go:306-307` reads `filter.Alias` for the account-listing filter (the `Account.Alias` routing handle).
- Instrument-field readers: `components/crm/adapters/mongodb/alias/alias_query.mongodb.go:153-168` (filter construction), `:64-65` and `components/crm/adapters/http/in/observability.go:38-39` (span flags), AND `components/ledger/internal/adapters/http/in/observability.go:207-208` (the LEDGER observability surface reads the same CRM-domain fields — wider blast radius than the macro plan stated).

**Change shape.** Audit-only; the output is a frozen field list written into `F2-EXECUTION-NOTE.md`. AUDIT RESULT (already measured at HEAD, recorded so F2-T13 cannot drift): `QueryHeader.Alias` IS dual-use (read by the account-listing path) → it STAYS (DO-NOT-TOUCH). The renamable instrument-domain fields are exactly: `BankingDetailsBranch`, `BankingDetailsAccount`, `BankingDetailsIban`, `RegulatoryFieldsParticipantDocument`, `RelatedPartyDocument`, `RelatedPartyRole`.

**Verification.** Narrowest: `grep -n 'filter.Alias' components/ledger/internal/adapters/postgres/account/account.postgresql.go` shows the account-listing read (proves dual-use). `grep -rn 'RelatedPartyDocument\|BankingDetailsBranch' --include='*.go' components | grep -v _test | grep -v httputils.go` enumerates every reader the rename must touch (CRM query + 2 observability surfaces).

**Dependencies.** None. Gates F2-T13.

**Notes.** Drift vs §6 Cross-component sweep: the plan said the instrument fields are "read by the CRM alias listing path via `observability.go:38-39`". HEAD shows a THIRD reader: the ledger-side `observability.go:207-208`. F2-T13 must edit all three reader sites, not just the CRM one. Confirms Risk 11 was right to mandate this audit.

---

### F2-T03 — Namespace-target proposal (resource `instruments`, related-parties sub-resource)

**Objective.** Settle the authz-target decision the macro plan defers to "F2 task expansion": pick the namespace the flipped CRM routes authorize under, and the resource names.

**Touchpoints (HEAD, verified).**
- `components/crm/adapters/http/in/routes.go:21` (`const ApplicationName = "plugin-crm"`); resource literals `"holders"` (`:86-90`) and `"aliases"` (`:93-98`).
- F0 four-namespace inventory (`midaz`, `routing`, `plugin-crm`, `plugin-fees`): the ledger's own namespace is `midaz` (`components/ledger/internal/adapters/http/in/routes.go:25-28`).
- `docs/auth/RBAC-NAMESPACES.md:9,23,39` documents the keying contract and the X1 migration cost.

**Change shape.** Decision record in `F2-EXECUTION-NOTE.md`, no code. DECISION (default candidate, binding unless overridden): namespace `midaz` (the ledger's own, from the F0 inventory); resource `instruments` replaces `"aliases"`; `holders` resource unchanged; related-parties stay UNDER the `instruments` resource as sub-resource maintenance (Q8-RESOLVED — one fewer resource in the X1 policy matrix). The constant `ApplicationName` is removed/repointed in F2-T12.

**Verification.** Narrowest: the note records `{namespace, holders-resource, instruments-resource, related-parties-grouping}` with the F0-inventory citation. No build check (decision artifact).

**Dependencies.** None. Gates F2-T11, F2-T12.

**Notes.** This call is load-bearing for X1: the chosen namespace/resource matrix is exactly what Fred + the plugin-auth team migrate policies to. Reversible only at cost (re-issuing grants), so the proposal is explicit, not implicit.

---

### F2-T04 — Rename `pkg/mmodel/alias.go` → `instrument.go`; rename the type + input + `@name` set

**Objective.** Hard-rename the colliding CRM entity at its source: `mmodel.Alias` → `mmodel.Instrument`, inputs and `@name` swagger identifiers, in the `mmodel` package.

**Touchpoints (HEAD, verified).** `pkg/mmodel/alias.go`:
- `CreateAliasInput` (`:45`, `@name CreateAliasRequest` `:58`) → `CreateInstrumentInput` / `@name CreateInstrumentRequest`.
- `UpdateAliasInput` (`:64`, `@name UpdateAliasRequest` `:73`) → `UpdateInstrumentInput` / `@name UpdateInstrumentRequest`.
- `Alias` (`:79`, `@name AliasResponse` `:93`) → `Instrument` / `@name InstrumentResponse`.
- Doc comments at `:13,22,41,60,75` reference "alias"; `RegulatoryFields` (`:17`), `RelatedParty` (`:26`), `BankingDetails` (`:99`) are nested types — keep their names, update only doc prose that says "for an alias".
- `Date` type (`pkg/mmodel/date.go`) is referenced by `RelatedParty.StartDate/EndDate` — untouched.

**Change shape.** Rename file `pkg/mmodel/alias.go` → `pkg/mmodel/instrument.go` (snake_case rule satisfied); rename the three exported types + their `@name` tags + swagger-model comment lines. Nested types `RegulatoryFields`/`RelatedParty`/`BankingDetails` keep their names (not part of the collision). Single-file edit + dependent compile fan-out handled by F2-T05/06/07.

**Verification.** Narrowest: `test -f pkg/mmodel/instrument.go && ! test -f pkg/mmodel/alias.go`; `grep -n 'type Instrument struct\|CreateInstrumentInput\|UpdateInstrumentInput\|@name InstrumentResponse' pkg/mmodel/instrument.go` returns the renamed declarations. Build: `go build ./pkg/mmodel/` (will pass; downstream packages break until F2-T05/06/07).

**Dependencies.** F2-T01. Gates F2-T05, F2-T06, F2-T07, F2-T09, F2-T10.

**Notes.** Holder is NOT renamed (Q6/Design call 4 — zero coupling, verified). `BankingDetails` is verified NOT embedded by `holder.go`.

---

### F2-T05 — Rename Mongo adapter package `alias/` → `instrument/`

**Objective.** Rename the CRM Mongo adapter directory, its files, and the `*mmodel.Alias` references in the model mapping, so the renamed entity compiles through the persistence layer. The PHYSICAL collection name `aliases_<orgID>` stays (Design call 1 / Q4).

**Touchpoints (HEAD, verified).** Dir `components/crm/adapters/mongodb/alias/` (4 non-test + 1 mock + 4 test):
- `alias.go` (model `MongoDBModel` `:16`; `FromEntity(a *mmodel.Alias, ...)` `:176`; `ToEntity(...) (*mmodel.Alias, error)` `:242`).
- `alias.mongodb.go` (`type Repository interface` `:37`, `type MongoDBRepository struct` `:48`; 4 collection literals `strings.ToLower("aliases_"+organizationID)` at `:109,189,246,349` — KEEP the literal string).
- `alias_query.mongodb.go` (2 collection literals `:45,211` — KEEP).
- `alias_maintenance.mongodb.go` (1 collection literal `:50` — KEEP; `DeleteRelatedParty`).
- `alias.mongodb_mock.go` (gomock for the interface).
- tests: `alias.mongodb_test.go`, `alias.mongodb_integration_test.go`, `alias_test.go`, `alias.mongodb_tenant_test.go`.

**Change shape.** `git mv` the directory `alias/` → `instrument/`; rename the package declaration `package alias` → `package instrument`; rename files `alias*.go` → `instrument*.go`; update `*mmodel.Alias` → `*mmodel.Instrument` in `FromEntity`/`ToEntity`; update every importer's `alias.Repository` selector to `instrument.Repository`. The 7 collection literals `aliases_` stay byte-for-byte. Regenerate the mock (`alias.mongodb_mock.go` → `instrument.mongodb_mock.go`) or hand-rename if generation is unavailable.

**Verification.** Narrowest: `test -d components/crm/adapters/mongodb/instrument && ! test -d components/crm/adapters/mongodb/alias`; `grep -rn 'aliases_' components/crm/adapters/mongodb/instrument/ | grep -v _test | wc -l` returns 7 (literals preserved). Build: `go build ./components/crm/adapters/mongodb/...`.

**Dependencies.** F2-T04. Gates F2-T06, F2-T16.

**Notes.** Risk 4 safeguard: this dir holds the model + repo; missing it leaves a half-renamed surface. The collection-literal preservation is the single most error-prone line — Gate 7 (F2-T16) and Gate 5 prove it. Design call 1 is load-bearing (Q4: production data exists in `aliases_*`).

---

### F2-T06 — Rename CRM service files + `AliasRepo` field + `delete-related-party.go` span attr

**Objective.** Rename the 8 alias-domain service files (including the two related-party services), the `AliasRepo` field on the CRM `UseCase`, and the stale `app.request.alias_id` span attribute.

**Touchpoints (HEAD, verified).** `components/crm/services/` (8 non-test + 8 test mirrors):
- `create-alias.go`, `update-alias.go`, `delete-alias.go`, `get-id-alias.go`, `get-all-aliases.go`, `validate-alias-closing-date.go`.
- `delete-related-party.go` (`DeleteRelatedPartyByID`, calls `AliasRepo.DeleteRelatedParty`; span attr `app.request.alias_id` at `:28`), `validate-related-party.go`.
- `service.go:14`: `AliasRepo alias.Repository` → `InstrumentRepo instrument.Repository`.

**Change shape.** `git mv` each `*-alias*.go` / `*-related-party*.go` to its `*-instrument*` / kept-name equivalent (related-party files keep their `related-party` filename — they are a sub-resource, Q8); rename `AliasRepo` field + every call-site selector; repoint span attr `app.request.alias_id` → `app.request.instrument_id` at `delete-related-party.go:28`. Update the `_test.go` mirrors for compile. Do NOT touch logging-compliance debt (`fmt.Sprintf`-in-logger, `LevelInfo`) — explicitly out of F2 scope.

**Verification.** Narrowest: `grep -rn 'AliasRepo\b' components/crm --include='*.go'` returns zero; `grep -n 'app.request.instrument_id' components/crm/services/delete-related-party.go` returns the repointed attr. Build: `go build ./components/crm/services/...`.

**Dependencies.** F2-T04, F2-T05. Gates F2-T07.

**Notes.** Risk 4: `delete-related-party.go` does NOT reference `mmodel.Alias`, so Gate 1's grep does not catch it — the named inventory is the safeguard. CLAUDE.md compliance cleanup is deliberately deferred (§6 Out of scope) so the implementer does not opportunistically rewrite logging mid-rename.

---

### F2-T07 — Rename CRM handler `alias.go` → `instrument.go`; handler type + methods + swag annotation text

**Objective.** Rename the HTTP handler file, the `AliasHandler` type, its 6 methods, and the in-place swag annotation text (NO spec regeneration — that is F5).

**Touchpoints (HEAD, verified).** `components/crm/adapters/http/in/alias.go`:
- `type AliasHandler struct` (`:24`); methods `CreateAlias` (`:44`), `GetAliasByID` (`:98`), `UpdateAlias` (`:158`), `DeleteAliasByID` (`:233`), `GetAllAliases` (`:304`), `DeleteRelatedParty` (`:385`).
- swag annotations: `@Tags Aliases` (`:32,86,145,222,280,374`); `@Param ... mmodel.CreateAliasInput` (`:38`) / `mmodel.UpdateAliasInput` (`:152`); `@Success {object} mmodel.Alias` (`:39,93,153`) / `http.Pagination{items=[]mmodel.Alias}` (`:299`); `@Router /v1/holders/{holder_id}/aliases*` and `/v1/aliases` (`:43,97,157,232,303,384`).
- Wiring references: `aliasHandler`/`ah *AliasHandler` in `routes.go:29,68,84,105`, `config.go:839` (`crmMgo.aliasHandler`), `crm_collapse_integration_test.go`.

**Change shape.** `git mv alias.go instrument.go`; rename type → `InstrumentHandler`, methods → `CreateInstrument`/`GetInstrumentByID`/`UpdateInstrument`/`DeleteInstrumentByID`/`GetAllInstruments`/`DeleteRelatedParty` (the related-party method keeps its name — sub-resource); rewrite `@Tags Aliases`→`Instruments`, `mmodel.CreateAliasInput`→`...Instrument`, `mmodel.Alias`→`mmodel.Instrument`, `@Router` `/aliases`→`/instruments`. F2 changes ANNOTATION TEXT only; the generated `components/crm/api/*` set stays stale until F5. Update the `aliasHandler` field/param identifiers in the wiring files for compile (routes done in F2-T11).

**Verification.** Narrowest: `test -f components/crm/adapters/http/in/instrument.go`; `grep -n 'InstrumentHandler\|@Tags\s*Instruments\|@Router.*instruments' components/crm/adapters/http/in/instrument.go` returns the renamed surface. Build: `go build ./components/crm/adapters/http/in/...` (route grep-zero deferred to F2-T11).

**Dependencies.** F2-T04, F2-T06. Gates F2-T11.

**Notes.** §6 (e) boundary: F2 edits annotation source; F5 runs `swag` and produces the unified artifact (the `make generate-docs` crm leg is broken at HEAD — phase Risk 8). Do NOT regenerate in F2.

---

### F2-T08 — Add `constant.EntityInstrument` + `constant.EntityRelatedParty` (+ `EntityHolder`)

**Objective.** Introduce the explicit entity-name constants (Q5-RESOLVED) so the error `entity` field is declared, not type-derived.

**Touchpoints (HEAD, verified).** `pkg/constant/entity.go:9-24` — the `EntityFoo` block (currently `EntityAccount`..`EntityTransactionRoute`, NO holder/alias/related-party). Convention established at `:10`.

**Change shape.** Add `EntityInstrument = "Instrument"`, `EntityRelatedParty = "RelatedParty"`, and `EntityHolder = "Holder"` to the const block (alphabetical placement consistent with the existing ordering). Small additive edit.

**Verification.** Narrowest: `grep -n 'EntityInstrument\|EntityRelatedParty\|EntityHolder' pkg/constant/entity.go` returns the 3 new constants. Build: `go build ./pkg/constant/`.

**Dependencies.** None (independent of the rename — the constant strings are literals). Gates F2-T09.

**Notes.** `EntityInstrument = "Instrument"` makes the externally-visible error-`entity` value flip from `"Alias"` to `"Instrument"` deliberate (Risk 6/R43). `EntityHolder` is included because the holder error site (`delete-holder.go:47`) also carries the banned `reflect.TypeOf` pattern and F2 touches its neighbourhood; F1 may also introduce `EntityHolder` — if F1 lands first, this task only adds the two instrument/related-party constants and skips the duplicate (resolve at execution time, see Notes in F2-T09).

---

### F2-T09 — Replace the 14 `reflect.TypeOf` sites with the new constants

**Objective.** Eliminate the banned `reflect.TypeOf(mmodel.X{}).Name()` anti-pattern at all 14 production sites (7 former-`Alias` + 7 `RelatedParty`), substituting the F2-T08 constants.

**Touchpoints (HEAD, verified).**
- 7 former-`Alias` (now `Instrument`) sites → `constant.EntityInstrument`: `alias.mongodb.go:149,207,296,372,389`, `alias_maintenance.mongodb.go:77`, `validate-alias-closing-date.go:44` (post-T05/T06 these live under `instrument/` and renamed service files).
- 7 `RelatedParty` sites → `constant.EntityRelatedParty`: `alias_maintenance.mongodb.go:81`, `validate-related-party.go:35,42,47,52,57,62`.

**Change shape.** At each site replace `reflect.TypeOf(mmodel.Alias{}).Name()` → `constant.EntityInstrument` and `reflect.TypeOf(mmodel.RelatedParty{}).Name()` → `constant.EntityRelatedParty`. Drop the now-unused `reflect` import where it becomes orphaned. Pure substitution.

**Verification.** Narrowest: `grep -rn 'reflect.TypeOf(mmodel.Alias' --include='*.go' | grep -v _test` AND `grep -rn 'reflect.TypeOf(mmodel.RelatedParty' --include='*.go' | grep -v _test` both return zero. `grep -rn 'constant.EntityInstrument\|constant.EntityRelatedParty' components/crm --include='*.go' | grep -v _test | wc -l` returns 14. Build: `go build ./components/crm/...`.

**Dependencies.** F2-T04 (type rename — `mmodel.Instrument` must exist), F2-T08 (constants). Gates F2-T15 (error/entity tests).

**Notes.** Gate 4 requires BOTH greps zero AND both constants used — a narrow Alias-only gate would let a RelatedParty site survive (phase Risk 7). The 7 RelatedParty sites keep serializing `"RelatedParty"` (value unchanged) but the banned pattern is removed. If F1 already added `EntityHolder`, do not redeclare it; this task only adds the instrument/related-party usages.

---

### F2-T10 — Rename CRM-prefixed error sentinel `ErrHolderHasAliases` (+ ref) and alias-named sentinels

**Objective.** Rename the CRM-prefixed sentinels whose Go IDENTIFIER carries "Alias" so the rename is internally consistent. The CRM-00xx CODE STRINGS (e.g. `"CRM-0008"`) stay — they are the wire contract; only the Go identifier name changes.

**Touchpoints (HEAD, verified).** `pkg/constant/errors.go:226-241`:
- `ErrAliasNotFound` (`:227`, code `CRM-0008`) → `ErrInstrumentNotFound`.
- `ErrHolderHasAliases` (`:230`, code `CRM-0017`) → `ErrHolderHasInstruments` (the D-1 tail flagged in F1 scope so Risk R1's reference does not rot).
- `ErrAliasClosingDateBeforeCreation` (`:235`, code `CRM-0023`) → `ErrInstrumentClosingDateBeforeCreation`.
- Reference site: `components/crm/services/delete-holder.go:47` (`ErrHolderHasAliases`, also carries `reflect.TypeOf(mmodel.Holder{}).Name()` — replace with `constant.EntityHolder` if F2-T08 added it).
- `ErrRelatedParty*` (`:236-241`) keep their identifiers (related-party sub-resource, not renamed).
- `pkg/constant/errors_crm_test.go` and `pkg/errors.go` (ValidationError map ~`:1178-1186`) reference these identifiers — update for compile.

**Change shape.** Rename the 3 alias-named identifiers; KEEP the `errors.New("CRM-00xx")` code strings byte-for-byte. Update the `delete-holder.go:47` call site and the `errors_crm_test.go` / `pkg/errors.go` map entries. The DO-NOT-TOUCH numeric sentinels (F2-T01) are untouched.

**Verification.** Narrowest: `grep -rn 'ErrAliasNotFound\|ErrHolderHasAliases\|ErrAliasClosingDateBeforeCreation' --include='*.go'` returns zero (outside CHANGELOG/migration-guide). `grep -n '"CRM-0008"\|"CRM-0017"\|"CRM-0023"' pkg/constant/errors.go` confirms the codes survive. Build: `go build ./pkg/... ./components/crm/...`.

**Dependencies.** F2-T04. Independent of routes.

**Notes.** Drift confirmation: `delete-holder.go:47` is the ONLY non-test reference to `ErrHolderHasAliases` (verified). The wire-visible error CODE is unchanged; only the entity-type field flips (handled by F2-T09). This closes the §6 boundary item flagged in F1's Out-of-scope ("F2 must also rename `ErrHolderHasAliases`").

---

### F2-T11 — Routes hard cut: `/aliases*` → `/instruments*`, no dual-serve

**Objective.** Execute D-10's route hard cut: the 6 alias route registrations become `/v1/holders/:holder_id/instruments*` (+ `/v1/instruments`), with NO old path surviving.

**Touchpoints (HEAD, verified).** `components/crm/adapters/http/in/routes.go:93-98` (the 6 alias registrations):
- `GET /v1/aliases` (`:93`) → `GET /v1/instruments`.
- `POST /v1/holders/:holder_id/aliases` (`:94`).
- `GET /v1/holders/:holder_id/aliases/:alias_id` (`:95`).
- `PATCH .../aliases/:alias_id` (`:96`).
- `DELETE .../aliases/:alias_id` (`:97`).
- `DELETE .../aliases/:alias_id/related-parties/:related_party_id` (`:98`).
- Handler refs `ah.CreateAlias`→`ah.CreateInstrument` etc. (post-T07); body binders `mmodel.CreateAliasInput`→`...Instrument`; `ParseUUIDPathParameters("aliases")`/`("related-parties")` span labels → `"instruments"`/`"related-parties"`.
- Single edit point covers both callers: `NewRouter` (`:68`) and `CreateCRMRouteRegistrar` (`:105`) both call `RegisterCRMRoutesToApp` (`:84`).

**Change shape.** Edit the 6 registration lines in `RegisterCRMRoutesToApp`: path literals `aliases`→`instruments`, path-param `:alias_id`→`:instrument_id`, handler method names (post-T07), body-binder types (post-T04), and the `ParseUUIDPathParameters` label. No second serving block (D-10 hard cut). Authz literals are flipped in F2-T12 (same lines, separated for review clarity).

**Verification.** Narrowest: `grep -rn '/v1/aliases\|/v1/holders/:holder_id/aliases' components/crm components/ledger --include='*.go'` (excluding `components/crm/api/`) returns zero. `grep -n 'instruments' components/crm/adapters/http/in/routes.go` shows the 6 renamed routes. Build: `go build ./components/crm/... ./components/ledger/...`.

**Dependencies.** F2-T07 (handler methods), F2-T03 (namespace target). Gates F2-T14, F2-T17.

**Notes.** The `ParseUUIDPathParameters("aliases")` literal is a span-attribute label only — the user-facing path-param error uses the param name (`:alias_id`→`:instrument_id`), so renaming the label is cosmetic-consistency, not a behaviour change. Gate 2 grep-zero excludes `components/crm/api/` (F5-owned generated residue, phase Risk 10 / R10).

---

### F2-T12 — Authz flip: `plugin-crm` namespace + `"aliases"` resource literal removed in code

**Objective.** Execute the Q3-RESOLVED in-code namespace flip: remove `plugin-crm` and the `"aliases"` resource literal from the codebase, repointing CRM routes to the F2-T03 target.

**Touchpoints (HEAD, verified).** `components/crm/adapters/http/in/routes.go`:
- `const ApplicationName = "plugin-crm"` (`:21`).
- `auth.Authorize(ApplicationName, "holders", ...)` at `:86-90` (holders resource).
- `auth.Authorize(ApplicationName, "aliases", ...)` at `:93-98` (6 sites).
- Doc comment `:82` ("plugin-crm via ApplicationName").
- Post-F1 surface (not at HEAD): F1-T13 registers `GET /v1/holders/:id/accounts` under the same `ApplicationName` constant (resource `"holders"`), so at F2-T12 execution time the constant carries one more route than this HEAD inventory. The constant repoint moves it automatically — no extra edit needed (R16 handoff; see F1-T13 notes). The verification grep below catches any straggler regardless.

**Change shape.** Per F2-T03 decision: repoint `ApplicationName` to the target namespace (default `midaz`) or replace the constant with the ledger's namespace constant import; change the 6 `"aliases"` resource literals to `"instruments"`; `"holders"` resource stays. Related-parties stay under the `instruments` resource (Q8 — the related-parties DELETE authorizes with resource `"instruments"`, verb `delete`). Update the `:82` doc comment.

**Verification.** Narrowest: `grep -rn '"plugin-crm"' components/ pkg/ --include='*.go'` (excluding `components/crm/api/`) returns zero. `grep -n 'Authorize(' components/crm/adapters/http/in/routes.go` shows no `"aliases"` literal and the target namespace at all CRM sites. Build: `go build ./components/crm/... ./components/ledger/...`.

**Dependencies.** F2-T03 (namespace target), F2-T11 (route paths). Gates F2-T17.

**Notes.** R1/X1: this flip orphans every tenant's `plugin-crm:*` grant in the external auth-server. The policy migration is Fred-owned with the plugin-auth team at v4 finalization (X1, release/deploy gate) — NOT a merge blocker. Local/dev with auth disabled are unaffected. F2-T17 records the flip in `docs/auth/RBAC-NAMESPACES.md` and the migration guide.

---

### F2-T13 — `QueryHeader` instrument-domain filter-field rename (audit-scoped)

**Objective.** Rename the instrument-domain filter fields on the shared `QueryHeader` for query-parameter consistency on the instruments surface (Q7-RESOLVED), STRICTLY scoped to the fields F2-T02 proved single-use.

**Touchpoints (HEAD, verified).** `pkg/net/http/httputils.go` — the 6 audit-approved fields: `BankingDetailsBranch` (`:52`), `BankingDetailsAccount` (`:53`), `BankingDetailsIban` (`:54`), `RegulatoryFieldsParticipantDocument` (`:56`), `RelatedPartyDocument` (`:57`), `RelatedPartyRole` (`:58`). The shared `Alias *string` (`:70`) is DUAL-USE (F2-T02) → NOT renamed. Reader sites that must follow the rename:
- `components/crm/adapters/mongodb/alias/alias_query.mongodb.go:64-65,153-168` (filter + span).
- `components/crm/adapters/http/in/observability.go:38-39` (span flags).
- `components/ledger/internal/adapters/http/in/observability.go:207-208` (ledger-side span flags — the third reader the macro plan missed; F2-T02 drift note).

**Change shape.** Rename the 6 field identifiers on `QueryHeader` to the instrument-consistent names settled at F2-T02 (the JSON query-param binding follows from the struct tag/field; preserve the wire `?param=` names unless F2-T02 explicitly approved a query-param rename). Update all three reader sites. Keep `Alias` field untouched.

**Verification.** Narrowest: `grep -n 'Alias \*string' pkg/net/http/httputils.go` still present (dual-use field survives). `grep -rn '<old-field-name>' --include='*.go' components pkg` returns zero for each renamed field. Broader: `go build ./...` (shared `pkg/net/http` touches every route surface — full build is the backstop, Risk 11).

**Dependencies.** F2-T02 (frozen field list). Independent of the route/authz tasks.

**Notes.** `pkg/net/http` is shared infra used by EVERY route surface — Risk 11. The dual-use `Alias` field staying put is the load-bearing safeguard against breaking account listing (`account.postgresql.go:306`). Full-suite green (F2-T18) is the backstop.

---

### F2-T14 — Update real router-registration integration tests to assert `/instruments*`

**Objective.** Update the tests that exercise the REAL CRM router so their path assertions hit `/v1/holders/:id/instruments*` and pass.

**Touchpoints (HEAD, verified).** `components/ledger/internal/bootstrap/crm_collapse_integration_test.go`: the 3 `RegisterCRMRoutesToApp` call sites at `:241,358,395` (`crm.holderHandler, crm.aliasHandler` / `holderHandler, aliasHandler` / `hh, ah`). Any path assertions in this file referencing `/aliases`.

**Change shape.** Update path assertions `/v1/holders/:id/aliases*`→`/instruments*`; update the `aliasHandler` field/var references to the renamed identifier (post-T07). These are real router pins — the assertions must reflect the cut, not just compile.

**Verification.** Narrowest: `go test ./components/ledger/internal/bootstrap/ -run TestCRMCollapse` (or the relevant `-run` for the 3 sites) passes; path assertions hit `instruments`. `grep -n 'aliases' components/ledger/internal/bootstrap/crm_collapse_integration_test.go` returns zero route-path hits.

**Dependencies.** F2-T11. Part of Gate 8.

**Notes.** §6 Gate 8 distinguishes these REAL router-registration tests from the fixture stubs (F2-T15). These exercise the actual mounted routes — coverage, not bookkeeping.

---

### F2-T15 — Update fixture-stub + rename-test bookkeeping

**Objective.** Update the hand-rolled fixture stubs and the entity-field-value tests that are string bookkeeping (not coverage), so the suite compiles and reflects the rename.

**Touchpoints (HEAD, verified).**
- `components/crm/adapters/http/in/routes_test.go:248,271`: hand-rolled `app.Get("/v1/aliases", ...)` 401-on-missing-tenant stubs — fixture strings only.
- `components/crm/adapters/http/in/error_contract_test.go`: asserts on Holder error contracts ONLY (`ErrHolderNotFound`, `reflect.TypeOf(mmodel.Holder{})` at `:170`) — it does NOT assert an Alias/Instrument entity value (DRIFT vs §6 Gate 8, see Notes). Update only if F2-T10's `ErrHolderHasAliases` rename or F2-T09's `EntityHolder` substitution touches a referenced symbol.
- `components/crm/adapters/http/in/alias_test.go` (→ `instrument_test.go`): asserts error CODES (`cn.ErrInvalidRelatedPartyRole.Error()` etc.) — update renamed identifiers + file name.

**Change shape.** Rename `routes_test.go` stub path strings `/v1/aliases`→`/v1/instruments`; rename `alias_test.go`→`instrument_test.go` and its `mmodel.Alias`/handler references; in `error_contract_test.go` update only symbol references the rename actually broke. Where a test SHOULD assert the flipped `entity` field value (`"Instrument"`), add that assertion in the instrument handler test rather than chasing the non-existent assertion in `error_contract_test.go`.

**Verification.** Narrowest: `go test ./components/crm/adapters/http/in/...` passes. `grep -rn '/v1/aliases' components/crm/adapters/http/in/*_test.go` returns zero.

**Dependencies.** F2-T11 (routes), F2-T09 (constants). Part of Gate 8.

**Notes.** DRIFT (recorded): §6 Gate 8 says "`error_contract_test.go` updated for the `entity` field value (`Alias`→`Instrument`)". At HEAD that file contains NO Alias entity-value assertion — it tests only Holder contracts. The Alias→Instrument `entity`-field change is observable through `pkg.ValidateBusinessError(..., entityType)` (`pkg/errors.go:342`), surfaced via the renamed handler's error responses; the right place to lock it is the instrument handler test, not `error_contract_test.go`. Do not fabricate an assertion in a test that never had one.

---

### F2-T16 — Reporter zero-diff confirmation (collection name unchanged)

**Objective.** Prove that keeping the physical collection name `aliases_<orgID>` (Design call 1 / Q4) leaves the reporter untouched — no edit, confirmed by diff.

**Touchpoints (HEAD, verified).**
- `components/reporter-manager/internal/services/get-data-source-details-by-id.go:445,457,485` — literal `"holders"`/`"aliases"` collection-name switch.
- `pkg/reporter/datasource/direct_provider_schema.go:19` (`pluginCRMDataSourceID = "plugin_crm"`), `:84` (prefix grouping `holders_*`/`aliases_*`), `:135` (prefix→logical-name derivation).

**Change shape.** NONE. This is a confirmation task: because F2-T05 preserved the 7 `aliases_` literals, the reporter's prefix-based discovery and the hardcoded `"aliases"` switch require no change.

**Verification.** Narrowest: `git diff --stat components/reporter-manager pkg/reporter` shows zero changes for the phase. `grep -n '"aliases"' components/reporter-manager/internal/services/get-data-source-details-by-id.go` still returns the 3 unchanged sites.

**Dependencies.** F2-T05 (collection literals preserved). Gate 7.

**Notes.** R42/phase Risk 5: a collection rename would have broken reporter discovery and saved templates referencing `plugin_crm.aliases`. Design call 1 makes this a non-event; this task is the proof, not a change.

---

### F2-T17 — Docs/postman sweep: instruments noun, namespace flip, X1 note

**Objective.** Update the in-repo human-facing docs and postman collection to the instruments noun and the namespace flip, and record the X1 release-gate coordination. (Spec regeneration and `llms*.txt` contract-surface alignment are F5; this task covers the prose/RBAC/postman residue F2 owns.)

**Touchpoints (HEAD, verified).**
- `docs/api/SCOPING.md:23,35,54` (`holders / aliases`, `POST /v1/holders/{holder_id}/aliases`).
- `docs/auth/RBAC-NAMESPACES.md:9,23,39` (`plugin-crm` namespace + `holders, aliases` resources) — update to the flipped namespace/resource and ADD the X1 migration note.
- postman `postman/MIDAZ.postman_collection.json:16143` (`"Aliases"` folder) + the `/aliases` raw URLs at `:16171,16369,16493,16622,16751,16881`.
- `AGENTS.md:47-48`, `STRUCTURE.md:47,97,131`, `CLAUDE.md` (CRM `holders/aliases` + `plugin-crm` prose).
- `llms.txt`/`llms-full.txt` carry alias/plugin-crm refs — the CONTRACT-SURFACE alignment is F5 (Gate 7 of F5); F2 only fixes prose that names the renamed Go entity if it appears, leaving the served-surface wording to F5. (Coordinate at execution time to avoid double-editing.)

**Change shape.** Prose + postman edits: `aliases`→`instruments` noun, route URLs, namespace/resource names; in `RBAC-NAMESPACES.md` document the flip + the X1 migration (RBAC-NAMESPACES.md:8-12 keying contract). DO NOT touch `Account.Alias` routing-handle postman items ("Get Account by Alias" `:2712`, "List Balances by Account Alias" `:3946`) — DO-NOT-TOUCH (F2-T01).

**Verification.** Narrowest: `grep -rn '/v1/holders/{holder_id}/aliases\|/v1/aliases' docs/api/SCOPING.md postman/MIDAZ.postman_collection.json` returns zero (excluding the routing-handle items). `grep -n 'plugin-crm' docs/auth/RBAC-NAMESPACES.md` appears only in the migration-note context.

**Dependencies.** F2-T11, F2-T12. Part of Gate 10.

**Notes.** Boundary with F5: F5 regenerates specs and aligns the served `:3002` contract + `llms*.txt` surface wording. F2 owns the prose/RBAC/postman in-repo residue tied directly to the rename. Migration-guide and CHANGELOG are the sanctioned homes for OLD-name residue (Gate 10).

---

### F2-T18 — Full-suite gate: tests, lint, grep-zero

**Objective.** Close the phase exit gates: full build/test/lint green and the entity-scoped grep-zero set clean (excluding F5-owned `components/crm/api/` and the migration-guide/CHANGELOG).

**Touchpoints.** Whole repo. Gate greps from §6 gates 1-4, 7, 10 + F2-T01 allowlist.

**Change shape.** No production edit (fix-only if a gate fails). Run the gate matrix and the allowlist guard.

**Verification (narrowest first, then broader).**
1. Go grep-zero: `grep -rn 'mmodel\.Alias\b\|mmodel\.CreateAliasInput\|mmodel\.UpdateAliasInput' --include='*.go' | grep -v 'components/crm/api/'` returns zero; `pkg/mmodel/instrument.go` exists.
2. Route grep-zero: `grep -rn '/v1/aliases\|/v1/holders/:holder_id/aliases' components/crm components/ledger --include='*.go' | grep -v 'components/crm/api/'` returns zero; `grep -rn '"plugin-crm"' components/ pkg/ --include='*.go' | grep -v 'components/crm/api/'` returns zero.
3. `@name` grep-zero: `grep -rn 'AliasResponse\|CreateAliasRequest\|UpdateAliasRequest' --include='*.go' | grep -v 'components/crm/api/'` returns zero.
4. Entity-contract: both `reflect.TypeOf(mmodel.Alias` and `reflect.TypeOf(mmodel.RelatedParty` production greps return zero; `EntityInstrument`+`EntityRelatedParty` used (14 sites).
5. DO-NOT-TOUCH survives (F2-T01 allowlist): the 6 numeric sentinels + `Account.Alias`/`Balance.Alias` + `QueryHeader.Alias` unchanged.
6. Build/test/lint: `make test-unit`, `make test-integration`, `make lint` all green.

**Dependencies.** All F2 tasks.

**Notes.** Gate 5 (Mongo MT) and Gate 7 (reporter zero-diff) are proven by F2-T05/F2-T16's verification; this task aggregates the cross-cutting greps + full suite. Grep-zero EXCLUDES `components/crm/api/` (R10 — F5 clears the generated swagger residue via regeneration; the `make generate-docs` crm leg is broken at HEAD, so F2 cannot regenerate it).

---

## 3. Gate-closure map

| Exit gate (§6) | Closed by |
|----------------|-----------|
| 1. Go grep-zero (`mmodel.Alias*` zero; file renamed) | F2-T04 (rename), F2-T05/T06/T07 (fan-out), F2-T18 (grep) |
| 2. Route grep-zero (`/aliases` zero) + authz literals (`plugin-crm`/`"aliases"` zero) | F2-T11 (routes), F2-T12 (authz), F2-T18 (grep) |
| 3. `@name` grep-zero | F2-T04 (`@name` tags), F2-T07 (handler annotations), F2-T18 (grep) |
| 4. Entity-name contract (14 reflect sites + 2 constants) | F2-T08 (constants), F2-T09 (substitution) |
| 5. Mongo accessibility incl. MT | F2-T05 (rename + literal preservation; integration/tenant suites) |
| 6. Streaming unchanged | (confirmed clean at HEAD — no streaming file references the CRM entity; F2-T18 full-suite is the backstop) |
| 7. Reporter unbroken (zero diff) | F2-T16 |
| 8. Route/integration tests green (real pins vs fixtures) | F2-T14 (real router pins), F2-T15 (fixture stubs + entity tests) |
| 9. Full suite + lint | F2-T18 (also F2-T13's `go build ./...` backstop for the QueryHeader rename) |
| 10. Old-name residue confined | F2-T17 (docs/postman), F2-T18 (grep-zero excluding migration-guide/CHANGELOG/`crm/api`) |

Gate 6 (streaming) has no rename task by design: §6 (f) confirmed `pkg/streaming/events` has zero CRM-entity references at HEAD (only `Account.Alias`/`Balance.Alias` struct fields, which are DO-NOT-TOUCH). F2-T01 guards those fields; F2-T18's full suite re-proves the JSONShape tests stay green. No gate is left without a closing task.

---

## 4. Risk-to-task map

| Risk (§11) | Owning posture in F2 | Carried by |
|------------|----------------------|------------|
| R1 (Critical) — namespace flip orphans `plugin-crm:*` grants | In-code flip in F2; X1 is Fred-owned release gate | F2-T03 (target), F2-T12 (flip), F2-T17 (RBAC doc + X1 note), F2-T11 |
| R10 (High) — gates 2/3 unsatisfiable if `crm/api/` scanned | Grep-zero excludes `components/crm/api/` (F5-owned) | F2-T11, F2-T18 |
| R42 (Medium) — Mongo collection rename data-loss / reporter break | Design call 1: collection name unchanged | F2-T05 (literals preserved), F2-T16 (reporter zero-diff) |
| R43 (Medium) — `entity` error-body field flips `Alias`→`Instrument` | Intentional + explicit via constant | F2-T08, F2-T09, F2-T15 (test assertion), F2-T17 (migration guide) |
| Phase Risk 3 (High) — naive `alias` sweep corrupts DO-NOT-TOUCH | Entity-scoped greps + allowlist | F2-T01, F2-T04 (identifier-scoped), F2-T18 |
| Phase Risk 4 (High) — missing `delete-related-party.go`/`delete-alias.go` | Named inventory, not grep | F2-T05, F2-T06 |
| Phase Risk 7 (Medium) — a RelatedParty reflect site survives | Both greps zero + both constants | F2-T08, F2-T09 |
| Phase Risk 8/9 (Medium) — broken/stale CRM doc-gen | F2 edits annotation text only; regen is F5 | F2-T07 (text only) |
| Phase Risk 11 (High) — Q7 rename hits shared `pkg/net/http`; `Alias` may be dual-use | Mandatory audit; scoped rename; full-suite backstop | F2-T02 (audit), F2-T13 (scoped rename) |

All §11 risks whose owning-phase column names F2 (R1, R10, R42, R43) are mapped. The phase-local risks (§6 Risks 1-11) collapse onto the same R-ids (1↔R1, 2↔R42, 5↔R42, 6↔R43, 10↔R10) plus the rename-mechanics risks 3/4/7/8/9/11 mapped above. No owned risk is unmapped.

---

## 5. Execution-notes convention

`docs/v4/plan/F2-EXECUTION-NOTE.md` is created DURING execution: it records decisions made and commit SHAs as tasks land (the F2-T03 namespace target, the F2-T02 frozen field list, the F2-T01 allowlist, and any HEAD-drift adjustments), mirroring `docs/monorepo/plan/P2a-EXECUTION-NOTE.md`.
