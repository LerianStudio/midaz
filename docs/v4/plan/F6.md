# F6 — Module v4, sweep & release (task breakdown)

> **Status:** task breakdown of `docs/v4/PLAN.md` §10. The macro plan is authoritative for scope and gates; this file decomposes that scope into atomic, independently-verifiable tasks. Every count and residue location below is a **baseline measured at current HEAD** (`2bdbfe556`, where D-1/D-10 are UNEXECUTED) and MUST be re-measured at F6 execution time after F1–F5 merge — the exit gates, which run at execution time, are authoritative, not these baselines. Where HEAD has drifted from the macro plan's HEAD-pinned evidence, the task is written against HEAD truth and the drift is noted in the task and in the execution-note convention (§5).
> - **Date:** 2026-06-04
> - **Branch:** `feat/monorepo-consolidation` (Q1-RESOLVED: all v4 work continues here; the F0 gate-zero SHA is the v4 epoch marker / diff base).

F6 is the terminal mechanical major. It owns **no behaviour change**: every line it touches is a path/string rewrite, a deletion, a doc, a release-engineering step, or a re-run proof. It depends on F1–F5 ALL merged. The work splits into two commits by design (DC-1): one mechanical Go-import-rewrite commit (`go.mod` + ~7.1k import lines), then a human-doc/config sweep commit.

**HEAD drift recorded during verification (binding for the tasks below):**
1. **Import count re-measured:** ~7,127 occurrences across ~1,503 `.go` files at HEAD (plan baseline cited ~7,116 / ~1,501) — both move once F1–F5 merge. The occurrence count is the load-bearing figure; the gate runs at execution time.
2. **Latest git tag is `v3.8.0-beta.9`, NOT a clean `3.x` stable.** semantic-release's last-tag input is a `3.8.0` prerelease line, not `3.7.x`. This sharpens R5/Gate 10: the dry-run must be confirmed against the actual last tag on the release branch, and a `BREAKING CHANGE` from a `3.8.0-beta.N` base still computes `4.0.0` only if the major bump is honoured — verify, do not assume.
3. **Tracer spec version is NOT "templated only" (plan §10 scope row is wrong).** `components/tracer/api/docs.go:14` carries `"version": "{{.Version}}"` (the embedded doc-template), but the SAME file at `:3124` carries a HARDCODED `Version: "0.1.0"`, and `swagger.json:12`, `swagger.yaml:814`, `openapi.yaml:7` all carry literal `0.1.0`. Tracer has 4 hardcoded `0.1.0` literals to normalize — identical shape to reporter's `1.2.0` — not a no-op. F5 owns the version-train normalization (S12); F6 confirms it landed.
4. **Non-Go `/v3` doc surface re-measured:** 47 tracked non-`.go` files carry `midaz/v3` at HEAD (plan baseline cited 50). The v4 plan documents themselves now self-reference `midaz/v3`, so this count is unstable and not load-bearing; the gate is grep-zero over the curated surface, not a count match.
5. **`plugin-crm` residue includes a container name, not just the namespace.** `tests/reporter/chaos/chaos-circuit-breaker_test.go:152` uses the literal `"plugin-crm-mongodb"` (a Docker container name for the CRM Mongo), and `config.go:837` carries it in a comment. The grep-zero sweep must distinguish the RBAC namespace string `"plugin-crm"` (F2 flips it) from the container/infra name `plugin-crm-mongodb` — the latter is a P9-CRM-teardown / test-fixture concern, not a D-10 namespace residue, and a blanket grep trips on it.
6. **Dockerfiles + `.github/` confirmed CLEAN of `midaz/v3` and `v3.7`** (all four Dockerfiles, all of `.github/`). The charter named them first; they need no rewrite. Stated to close the ask with the negative result.

---

## 1. Task summary table

| ID | Title | Depends on | Traces to (scope item / exit gate) | Risks mitigated |
|----|-------|-----------|-------------------------------------|-----------------|
| F6-T01 | Re-measure all HEAD-pinned counts post-F1–F5; record the execution-time baseline | F1–F5 merged | Scope HEAD-disclaimer; Gates 1/3 | R28 (§11), F6-R1 |
| F6-T02 | `go.mod` module line `/v3 → /v4` + atomic Go-import rewrite (ONE mechanical commit) | F6-T01 | Scope: go.mod bump, import rewrite; Gates 1/2 | R28, F6-R7 |
| F6-T03 | Verify the mechanical commit is path-only: build/vet green, go.mod-only module change | F6-T02 | Scope: import rewrite (DC); Gates 1/2 | R28, F6-R7 |
| F6-T04 | OTEL_LIBRARY_NAME sweep: six `/v3` values → `/v4`; tracer's wrong `LerianStudio/tracer` → monorepo path | F6-T02 | Scope: OTEL rows; Gate 4 | R27 |
| F6-T05 | App-version source bump `main.go:20` `v3.7.0 → v4.0.0`; regenerate ledger docs | F6-T02 | Scope: app-version + generated docs rows; Gate 3 | F6-R8 |
| F6-T06 | Confirm tracer/reporter spec version train at `4.0.0` (F5 deliverable); clear CRM api residue if it survives | F6-T05 | Scope: tracer/reporter version + CRM doc-set rows; Gate 3 | R31, F6-R8 |
| F6-T07 | Postman: collection `v3.7.0 → v4.0.0`, stale `:3000`/`:3001` env ports retired, backups deleted/excluded | (sweep commit) | Scope: postman row; Gate 3 | R50 |
| F6-T08 | `tests/helpers/env.go` `:3000`/`:3001` → `:3002` (confirm F0/F5 landed it under /v4) | (sweep commit) | Scope: stale test URLs row; Gate 3 | R26-adjacent |
| F6-T09 | Repoint `tests/helpers/chaos.go` `up-backend`/`down-backend` → `up`/`down` (confirm F0 landed it) | (sweep commit) | Scope: chaos targets row; Gate 5 | R26 |
| F6-T10 | `GOVERNANCE.md:46` strip the malformed `/v3` GitHub-link segment | (sweep commit) | Scope: GOVERNANCE row; Gate 3 | R49 |
| F6-T11 | Non-Go `/v3` doc/config sweep (STRUCTURE/AGENTS/CLAUDE/llms/PROJECT_RULES/tracer doc set, etc.) | F6-T02 | Scope: non-Go `/v3` surface row; Gate 3 | R28-adjacent |
| F6-T12 | Curated grep-zero residue sweep report (`mmodel.Alias`, `plugin-crm`, `/aliases`, ports, `LerianStudio/tracer`, `midaz/v3`, `v3.7.0`) with explicit DO-NOT-TOUCH carve-outs | F6-T02..F6-T11 | Scope: grep-zero DC; Gate 3 | R28, R30, F6-R12 |
| F6-T13 | Author v3→v4 migration guide: REST consumer table + Go-importer identifier/path table | F1–F5 merged | Scope: migration guide row; Gate 9 | R43, R46 |
| F6-T14 | Extend `build.yml` to upload `components/tracer/migrations/` to S3 (additive, ledger path untouched) | — | Scope: tracer S3 row (Q15); Gate 11 | R45 |
| F6-T15 | PD-5 double-entry sum-zero re-proof under /v4 | F6-T02 | Scope: PD-5 re-proof row; Gate 6 | (PD-5 third rail) |
| F6-T16 | F3 usage-drift crash re-proof under /v4 (incl. CHAOS multitenant pair) | F6-T02 | Scope: F3 re-proof row; Gate 7 | R3, R4 |
| F6-T17 | Full matrix green via root `make ci` (incl. `./tests/reporter`) | F6-T02..F6-T11 | Scope: full-matrix row (Q16); Gates 8/13 | R25 |
| F6-T18 | Release-version alignment: `BREAKING CHANGE` trigger commit + semantic-release dry-run computes `4.0.0` | F6-T02..F6-T17 | Scope: release-tag row (DC); Gate 10 | R5 |
| F6-T19 | Fan-out sanity: dry-run confirms 4 images, helm/gitops mappings carry no crm/plugin-fees keys | F6-T14 | Scope: build.yml fan-out row; Gate 11 | R6 |
| F6-T20 | DEPLOY-GATE: external lockstep register (X1/X2/X3/X4) sign-off before production tag | F6-T18, F6-T19 | Scope: DEPLOY-GATE + lockstep register rows; Gate 12 | R1, R6 |

Risk IDs reference `docs/v4/PLAN.md` §11 (consolidated register). Phase-local §10 risk numbers map to the same rows: §10-R1→F6-R1 (HEAD-count staleness, mitigated by F6-T01), §10-R2→R5, §10-R3→R6, §10-R4→R27, §10-R5→R25, §10-R6→R26, §10-R7→R28, §10-R8→F6-R8 (version source re-inject), §10-R9→R46, §10-R10→R50, §10-R11→R49, §10-R12→R30.

---

## 2. Tasks

### F6-T01 — Re-measure all HEAD-pinned counts post-F1–F5

**Objective.** Before any rewrite, re-measure every count and residue location the macro plan pinned at HEAD, because F1–F5 change the residue set (the alias→instrument rename body, the `plugin-crm` flip, the spec regeneration are F1–F5's deliverables). The execution-time numbers — not these baselines — drive the sweep.

**Touchpoints (HEAD baseline, to RE-MEASURE).**
- Import occurrences: HEAD `2bdbfe556` = 7,127 across 1,503 files (`grep -rn 'github.com/LerianStudio/midaz/v3' --include=*.go . | grep -vE '/\.bin/|coverage.out' | wc -l`).
- Non-Go `/v3` files: HEAD = 47 tracked (`git grep -l 'midaz/v3' -- ':!*.go' | wc -l`).
- `v3.7.0` census outside CHANGELOG: HEAD = 11 tracked files (ledger api ×4, crm api ×4, `main.go:20`, postman collection, `docs/monorepo/analysis/03-midaz-host.md:145`).
- `plugin-crm` / `mmodel.Alias` / `/aliases` residue: F1–F5 expected to remove the bulk; F2 flips the namespace and renames the entity.

**Change shape.** No code change. Run the census commands against the post-F1–F5 tip, write the numbers into `F6-EXECUTION-NOTE.md` as the authoritative execution-time baseline. Flag any residue F1–F5 were supposed to clear but did not (a defect to route back, not for F6 to absorb).

**Verification.**
```
grep -rn 'github.com/LerianStudio/midaz/v3' --include=*.go . | grep -vE '/\.bin/|coverage.out' | wc -l   # occurrences
grep -rln 'github.com/LerianStudio/midaz/v3' --include=*.go . | grep -vE '/\.bin/|coverage.out' | wc -l  # files
git grep -l 'midaz/v3' -- ':!*.go' | wc -l
git grep -ln 'v3\.7\.0' -- ':!CHANGELOG.md'
```

**Dependencies.** F1–F5 ALL merged (inter-phase, per §3 DAG: F6 is terminal). No intra-phase predecessor.

**Notes.** Mitigates R28 and §10-R1 (Critical: trusting HEAD numbers sweeps the wrong residue). This task exists because the plan's HEAD-vs-execution disclaimer is binding — the gates run at execution time, the Scope baselines do not.

---

### F6-T02 — `go.mod` bump + atomic Go-import rewrite (ONE mechanical commit)

**Objective.** Bump the module path `github.com/LerianStudio/midaz/v3 → /v4` and rewrite every internal import in a single tool-driven mechanical commit reviewable as "nothing but path strings changed."

**Touchpoints (HEAD, verified).**
- `go.mod:1` — `module github.com/LerianStudio/midaz/v3`. No `replace`/`retract` directives exist (`grep -nE 'replace|retract' go.mod` → NONE), so this is a pure import-path rewrite.
- ~7,127 occurrences across ~1,503 `.go` files (re-measure per F6-T01).

**Change shape.** Tool-driven, NOT a blanket `sed`: `go mod edit -module github.com/LerianStudio/midaz/v4`, then rewrite import paths (e.g. via `gofmt -r` / `goimports` over the matched paths), then `goimports -w` for ordering. The mechanical commit contains ONLY `go.mod:1` + import lines. The human-doc/config sweep (T04–T13) is a SEPARATE second commit (DC: keep the Go rewrite bisectable and reviewable as path-only).

**Verification.**
```
go build ./...                                      # compiles under /v4
git show --stat HEAD | tail -1                       # ~1,503 files in the mechanical commit
grep -rn 'github.com/LerianStudio/midaz/v3' --include=*.go . | grep -vE '/\.bin/|coverage.out' | wc -l   # 0
```

**Dependencies.** F6-T01 (the count is re-measured first). Inter-phase: F1–F5 merged.

**Notes.** Mitigates R28 and F6-R7 (tool slip catching `Account.Alias` or CHANGELOG history). DC: "Single atomic Go-import-rewrite commit, separate from the human-doc/config sweep commit." The rewrite operates on the import PATH `github.com/LerianStudio/midaz/v3`, never on bare `v3` or `alias`, so the routing-handle `Account.Alias` and CHANGELOG `v3.6.x`/`v3.7.x` are structurally untouched.

---

### F6-T03 — Verify the mechanical commit is path-only

**Objective.** Prove the mechanical commit changed nothing but the module line and import paths — `go.mod` is the only module-graph change, no `replace`/`retract` introduced, build and vet green.

**Touchpoints (HEAD, verified).**
- `go.mod` — module line + (confirmed) NO `replace`/`retract`.
- The mechanical commit produced by F6-T02.

**Change shape.** No edit. A diff audit + build/vet gate on the F6-T02 commit.

**Verification.**
```
git show <mechanical-commit> -- go.mod                 # ONLY the module line changed
grep -E 'replace|retract' go.mod                        # NONE
go build ./... && go vet ./...                          # green
# Confirm the commit body is import-path-only (no prose, no logic):
git show <mechanical-commit> --stat | grep -vE '\.go|go\.mod|go\.sum' || echo "PATH-ONLY"
```

**Dependencies.** F6-T02. Inter-phase: none.

**Notes.** Mitigates R28 (Gate 2: go.mod-only module change) and F6-R7. Gate 2 of §10. `go.sum` may legitimately change if `go mod tidy` runs; if so, confirm the only delta is the module-path self-reference, not a dependency-version change.

---

### F6-T04 — OTEL_LIBRARY_NAME sweep (six `/v3` + tracer's wrong value)

**Objective.** Fix the runtime telemetry-attribution strings a Go-import sweep MISSES: six `/v3` env values across three components, plus tracer's already-wrong `github.com/LerianStudio/tracer` value (which contains NO `/v3` to catch).

**Touchpoints (HEAD, verified — all eight values).**
- `components/ledger/.env:207` + `.env.example:207` — `.../midaz/v3/components/ledger`.
- `components/reporter-manager/.env:98` + `.env.example:98` — `.../midaz/v3/components/reporter-manager`.
- `components/reporter-worker/.env:80` + `.env.example:80` — `.../midaz/v3/components/reporter-worker`.
- `components/tracer/.env:108` + `.env.example:108` — `github.com/LerianStudio/tracer` (WRONG, pre-consolidation; no `/v3`).

**Change shape.** Sweep-commit edits: the six `/v3` values → `/v4`; the two tracer values → the monorepo path `github.com/LerianStudio/midaz/v4/components/tracer` (the correct attribution for the co-located tracer component). Eight lines across eight files.

**Verification.**
```
grep -rn "OTEL_LIBRARY_NAME" components/*/.env components/*/.env.example
# Gate 4: all six midaz values on /v4 and tracer on the monorepo module path (not LerianStudio/tracer):
grep -rn "OTEL_LIBRARY_NAME=github.com/LerianStudio/tracer" components/ || echo "TRACER-FIXED"
grep -rn "OTEL_LIBRARY_NAME=github.com/LerianStudio/midaz/v3" components/ || echo "NO-V3"
```

**Dependencies.** F6-T02 (rides the sweep commit, after the mechanical bump). Inter-phase: F0 recorded these (R27).

**Notes.** Mitigates R27. Gate 4 of §10. The tracer value is the trap: a `/v3→/v4` grep silently skips it. Both `/v3` strings and the literal `LerianStudio/tracer` are on the curated sweep target list (F6-T12).

---

### F6-T05 — App-version source bump + regenerate ledger docs

**Objective.** Bump the hand-edited swagger source version `v3.7.0 → v4.0.0` at `main.go:20` and regenerate the generated ledger doc set, so the source and the generated artifacts agree and the next `generate-docs` does not re-inject `v3.7.0`.

**Touchpoints (HEAD, verified).**
- `components/ledger/cmd/app/main.go:20` — `// @version		v3.7.0` (hand-edited; the generated docs derive from it).
- Generated set carrying `v3.7.0`: `components/ledger/api/swagger.json:18`, `swagger.yaml:3472`, `openapi.yaml:15`, `docs.go:12604`.

**Change shape.** Bump the source annotation `v3.7.0 → v4.0.0`, then run `make generate-docs` so the four generated files regenerate to `v4.0.0`. NOTE the F5/F6 boundary: F5 (DC-2) is the phase that drives the version train off a single `VERSION=v4.0.0` source and regenerates. If F5 already landed this, F6-T05 is a confirmation; if any `v3.7.0` survived into F6, this task closes it. Re-verify which at execution time.

**Verification.**
```
grep -n "@version" components/ledger/cmd/app/main.go         # v4.0.0
grep -rn 'v3\.7\.0' components/ledger/api/                    # 0
```

**Dependencies.** F6-T02. Inter-phase: F5 DC-2 (version train) — re-verify overlap.

**Notes.** Mitigates F6-R8 (source-vs-generated re-inject). Gate 3 of §10. On `make generate-docs`, `COMPONENTS=("ledger" "crm")` (`scripts/generate-docs.sh:16`) processes the ledger leg first: it regenerates ledger's swagger/openapi/docs.go to `v4.0.0` and succeeds, THEN the `crm` leg fails (deleted `components/crm/cmd/app/main.go`), which sets `overall_success=false` and `break`s the loop (`scripts/generate-docs.sh:233-238`), making the script exit non-zero and skip postman convert + verify. So ledger's version files DO regenerate even with the broken `crm` leg; the broken leg is NOT what gates ledger version regeneration. The F5/R12 fix (remove `crm` from `COMPONENTS`) is still the precondition for an end-to-end exit-0 run. Confirm the script runs end-to-end at execution time (Gate 3).

---

### F6-T06 — Confirm tracer/reporter spec version train at 4.0.0; clear CRM api residue

**Objective.** Confirm the tracer (`0.1.0`) and reporter-manager (`1.2.0`) spec sets are normalized to the `4.0.0` train (F5's S12 deliverable), and delete any surviving `components/crm/api/` residue (F5/D-10 expected to delete it; if it survives into F6, F6 deletes it).

**Touchpoints (HEAD, verified — DRIFT vs §10 scope row).**
- **Tracer is NOT "templated only" (plan §10 row is wrong).** `components/tracer/api/docs.go:14` = `"version": "{{.Version}}"` (embedded doc-template), but `docs.go:3124` = HARDCODED `Version: "0.1.0"`, plus `swagger.json:12`, `swagger.yaml:814`, `openapi.yaml:7` all literal `0.1.0`. Four hardcoded `0.1.0` literals — identical shape to reporter, NOT a no-op. (`api/types.go` carries no version string.)
- Reporter-manager: `swagger.json:8`, `swagger.yaml:627`, `openapi.yaml:7`, `docs.go:2469` all literal `1.2.0`.
- `components/crm/api/` STILL EXISTS at HEAD (`docs.go`, `openapi.yaml`, `swagger.json`, `swagger.yaml`) carrying `v3.7.0`/`localhost:4003`/`CRM API` — F5/S7 deletes the whole directory; confirm gone.

**Change shape.** No edit if F5 landed it — a confirmation gate. If `0.1.0`/`1.2.0` survive: regenerate via `make generate-docs` after F5's version-source fix; the regeneration rewrites all four files per component. If `components/crm/api/` survives: delete the entire directory.

**Verification.**
```
grep -rn '0\.1\.0' components/tracer/api/      # 0 (all four files on 4.0.0)
grep -rn '1\.2\.0' components/reporter-manager/api/   # 0
ls components/crm/api/ 2>&1 | grep -q 'No such' && echo "CRM-API-GONE"
```

**Dependencies.** F6-T05. Inter-phase: F5 S12 (tracer/reporter normalization) + S7 (CRM api deletion).

**Notes.** Mitigates R31 (the phantom-holder-refs premise) and F6-R8. Gate 3 of §10. DRIFT recorded: the §10 scope claim "tracer is templated, no literal bump" is FALSE — record in `F6-EXECUTION-NOTE.md` so the gate text and reality agree. If F5 missed the tracer `docs.go:3124` literal, that is an F5 defect surfaced here, not new F6 scope.

---

### F6-T07 — Postman: version bump, stale ports retired, backups handled

**Objective.** Bump the postman collection version `v3.7.0 → v4.0.0`, retire the stale split-service env ports (`onboardingPort=3000`, `transactionPort=3001`), and delete/exclude the 12 backup copies rather than rewrite them.

**Touchpoints (HEAD, verified).**
- `postman/MIDAZ.postman_collection.json:6` — `"version": "v3.7.0"`.
- `postman/MIDAZ.postman_environment.json:67` — `onboardingPort` value `"3000"`; `:109` — `transactionPort` value `"3001"`; both feed `{{baseUrl}}:{{...Port}}` at `:73`/`:115`.
- `postman/backups/` — 12 JSON files (the noisiest false-rewrite source).

**Change shape.** Sweep-commit edits: collection version → `v4.0.0`; retire the `:3000`/`:3001` env entries (unified ledger is `:3002`); delete or gitignore `postman/backups/*` so they stop polluting greps. The postman collection itself is largely regenerated by `sync-postman.sh` (F5 S11/S18) once the unified ledger spec contains holders — F6 confirms the version landed and the stale ports are gone.

**Verification.**
```
grep -n '"version"' postman/MIDAZ.postman_collection.json | head -1     # v4.0.0
grep -n '"3000"\|"3001"' postman/MIDAZ.postman_environment.json || echo "PORTS-GONE"
ls postman/backups/*.json 2>/dev/null | wc -l                            # 0 (deleted) or gitignored
```

**Dependencies.** Sweep commit (after F6-T02). Inter-phase: F5 S11/S13/S18 (postman regeneration).

**Notes.** Mitigates R50 (build-artifact/backup grep noise). Gate 3 of §10. DC: backups are deleted/excluded, NOT rewritten.

---

### F6-T08 — `tests/helpers/env.go` stale URLs confirm under /v4

**Objective.** Confirm the test-helper default base URLs point at `:3002` (not the dead `:3000`/`:3001`), already landed by F0-T02 and bundled into the F5 sweep (S14) — F6 confirms it survives under /v4.

**Touchpoints (HEAD, verified).**
- `tests/helpers/env.go:27` — `ONBOARDING_URL` default `http://localhost:3000` (pre-F0); `:28` — `TRANSACTION_URL` default `http://localhost:3001`.

**Change shape.** No edit expected (F0-T02 fixed it). A confirmation grep. If a rebase or merge reverted it, repoint both defaults `:3000`/`:3001` → `:3002`.

**Verification.**
```
grep -n "localhost:300[01]" tests/helpers/env.go || echo "CLEAN"
grep -n "localhost:3002" tests/helpers/env.go        # both defaults
```

**Dependencies.** Sweep commit. Inter-phase: F0-T02 (original fix), F5 S14 (sweep bundle).

**Notes.** Bundled into the §10 hard-cut sweep. Gate 3 of §10 (the `3000`/`3001` grep over `tests/helpers/env.go`).

---

### F6-T09 — Repoint `tests/helpers/chaos.go` dead make targets

**Objective.** Confirm (or fix) the chaos helper calls surviving make targets `up`/`down`, not the non-existent `up-backend`/`down-backend` removed in consolidation.

**Touchpoints (HEAD, verified).**
- `tests/helpers/chaos.go:43` — `runMake(root, "up-backend")`; `:49` — `runMake(root, "down-backend")`.
- Surviving targets confirmed: `Makefile:420` (`up:`), `Makefile:434` (`down:`).
- The ONLY executable references to the dead targets are these two chaos.go lines (verified — no other `.go`/`.sh`/`.mk`/Makefile hits).

**Change shape.** No edit expected (F0-T03 fixed it). A confirmation. If reverted: two-line repoint `"up-backend"`→`"up"`, `"down-backend"`→`"down"`.

**Verification.**
```
grep -nE 'up-backend|down-backend' Makefile mk/*.mk tests/helpers/chaos.go || echo "DEAD-TARGETS-GONE"
grep -n 'runMake(root, "up")\|runMake(root, "down")' tests/helpers/chaos.go   # 2 call sites
grep -nE "^(up|down):" Makefile                                                # targets exist
```

**Dependencies.** Sweep commit. Inter-phase: F0-T03 (original fix).

**Notes.** Mitigates R26. Gate 5 of §10. DC: "`up-backend`/`down-backend` repoint is in-scope for F6" — F0 owns the fix, F6 confirms grep-zero so v4 does not ship the helper broken.

---

### F6-T10 — Strip the malformed `/v3` GitHub link in GOVERNANCE.md

**Objective.** Fix the already-broken public-facing GitHub link that carries the `/v3` module suffix as a path segment (not a real GitHub URL).

**Touchpoints (HEAD, verified).**
- `GOVERNANCE.md:46` — `https://github.com/LerianStudio/midaz/v3/blob/main/CODE_OF_CONDUCT.md` (the `/v3` is not a valid GitHub path segment; the link is already broken).

**Change shape.** Sweep-commit one-line edit: strip `/v3` → `https://github.com/LerianStudio/midaz/blob/main/CODE_OF_CONDUCT.md`. (Strip, NOT bump to `/v4` — it is a GitHub repo URL, not a Go module path; a repo URL has no version segment.)

**Verification.**
```
grep -n "midaz/v3" GOVERNANCE.md || echo "STRIPPED"
grep -n "github.com/LerianStudio/midaz/blob/main/CODE_OF_CONDUCT.md" GOVERNANCE.md
```

**Dependencies.** Sweep commit. Inter-phase: none.

**Notes.** Mitigates R49 (Low). Gate 3 of §10. Distinct from F6-T11: this is a GitHub URL (strip the segment), whereas Go-module doc refs become `/v4`.

---

### F6-T11 — Non-Go `/v3` doc/config sweep

**Objective.** Rewrite the human-facing `midaz/v3` Go-module references in docs/config to `/v4`, in the sweep commit (separate from the mechanical Go commit).

**Touchpoints (HEAD baseline, 47 tracked non-`.go` files — RE-MEASURE per F6-T01).**
- Top-level: `STRUCTURE.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`, `docs/PROJECT_RULES.md`, `llms.txt`, `llms-full.txt`.
- Env examples: `components/ledger/.env.example`, `components/reporter-manager/.env.example`, `components/reporter-worker/.env.example` (the OTEL lines are F6-T04; other `/v3` refs here are this task).
- Tracer doc set: `components/tracer/AGENTS.md`, `components/tracer/CLAUDE.md`, `components/tracer/docs/CODING_STANDARDS.md`, `components/tracer/docs/PROJECT_RULES.md`, `components/tracer/llms-full.txt`, `components/tracer/llms.txt`.
- Reporter READMEs: `components/reporter-manager/README.md`, `components/reporter-worker/README.md`.
- EXCLUDED (DO-NOT-TOUCH): `CHANGELOG.md` (historical release links), `docs/monorepo/PLAN.md` + `docs/monorepo/analysis/*` (frozen P0–P9 historical artifacts), and the `docs/v4/` plan documents (they intentionally cite `/v3` as the bump SOURCE).

**Change shape.** Sweep-commit edits across the curated file list, rewriting `github.com/LerianStudio/midaz/v3` → `/v4` in prose/config. NOT a blanket `sed`: the frozen-history trees (`CHANGELOG.md`, `docs/monorepo/`) and the v4 plan docs are excluded.

**Verification.**
```
# Curated grep-zero (exclude frozen history + v4 plan source):
git grep -l 'midaz/v3' -- ':!*.go' ':!CHANGELOG.md' ':!docs/monorepo/' ':!docs/v4/' || echo "DOC-SURFACE-CLEAN"
```

**Dependencies.** F6-T02 (sweep commit follows the mechanical commit). Inter-phase: F1–F5 docs work may have touched some of these — re-measure.

**Notes.** Mitigates the doc-surface portion of R28. Gate 3 of §10. The frozen-history exclusion is the same discipline applied to CHANGELOG: a blanket rewrite corrupts the P0–P9 record.

---

### F6-T12 — Curated grep-zero residue sweep report

**Objective.** Produce the grep-zero confirmation report: each dead token returns zero after excluding gitignored artifacts, CHANGELOG/frozen-history, and the D-1-exempt `Account.Alias` routing handle. This is the F6 confirmation pass over whatever residue F1–F5 left behind.

**Touchpoints (HEAD baseline residue — F1–F5 expected to clear, RE-MEASURE).**
- `mmodel.Alias` entity: `pkg/mmodel/alias.go:79` (`type Alias struct`) — F2 renames to `Instrument`.
- `/aliases` routes: `components/crm/adapters/http/in/alias.go:43,97,157` (`@Router .../aliases`) — F2 hard-cuts.
- `plugin-crm` NAMESPACE: `components/crm/adapters/http/in/routes.go:21` (`const ApplicationName = "plugin-crm"`), `routes_test.go:29` — F2 flips.
- **`plugin-crm` NON-namespace residue (DRIFT — distinguish):** `tests/reporter/chaos/chaos-circuit-breaker_test.go:152` = `"plugin-crm-mongodb"` (Docker container name, NOT the RBAC namespace); `config.go:837` = a comment. These are P9-CRM-teardown / test-fixture concerns — a blanket `plugin-crm` grep trips on them. The grep-zero target is the namespace STRING `"plugin-crm"` at route-registration sites, not the container name.
- Stale ports: `postman/MIDAZ.postman_environment.json` `onboardingPort.*3000`/`transactionPort.*3001`; `tests/helpers/env.go` `:3000`/`:3001`.
- `LerianStudio/tracer` in env files (F6-T04).
- `midaz/v3` in non-Go docs/config (F6-T11).
- `v3.7.0` outside CHANGELOG AND `docs/monorepo/analysis/` — the analysis tree carries one `v3.7.0` at `docs/monorepo/analysis/03-midaz-host.md:145` (frozen historical; exclude it the way CHANGELOG is excluded, OR rephrase that one line in the F6-T11 doc sweep — pick one, do not let gate and scope contradict).

**Change shape.** No code change — a verification report written into `F6-EXECUTION-NOTE.md`. Each token + its exact exclusion set + the zero result. DO-NOT-TOUCH allowlist explicit: `Account.Alias`/`Balance.Alias` struct fields, the 6 numeric-coded `Account.Alias` sentinels, CHANGELOG `v3.6.x`/`v3.7.x` history, `docs/monorepo/analysis/` frozen tree, `plugin-crm-mongodb` container name.

**Verification.**
```
grep -rn 'mmodel\.Alias\b' --include=*.go . | grep -vE '/\.bin/' || echo "ALIAS-ZERO"
grep -rn '"plugin-crm"' --include=*.go components/ pkg/ || echo "NS-ZERO"
grep -rn '/v1/.*/aliases' --include=*.go components/ pkg/ || echo "ROUTE-ZERO"
grep -rn 'v3\.7\.0' . --include=*.go --include=*.json --include=*.yaml --include=*.md | grep -vE 'CHANGELOG.md|docs/monorepo/analysis/|/\.bin/' || echo "V370-ZERO"
```

**Dependencies.** F6-T02..F6-T11 (all rewrites + the residue F1–F5 left). Inter-phase: F1–F5 own the rename/flip bodies; F6 confirms zero.

**Notes.** Mitigates R28, R30, and F6-R12 (Gate-3-vs-scope contradiction on the analysis doc). Gate 3 of §10. DC: "Sweep operates on grep-zero of a curated target list, not a blanket `/v3` rewrite." The `plugin-crm-mongodb`-vs-`plugin-crm` distinction is the new drift fact — record it so the gate does not red on a container name.

---

### F6-T13 — Author v3→v4 migration guide

**Objective.** Author the migration guide that ships in the release notes (D-10): a REST-consumer table (aliases→instruments routes, new holder fields/ownership reads, the tracer reservation API) and a Go-importer table (`mmodel.Alias`→`mmodel.Instrument` + nested types, the `/v3`→`/v4` path bump).

**Touchpoints (HEAD).**
- New artifact (location settled at task expansion; candidate `docs/v4/MIGRATION-v3-to-v4.md` or release-notes body, consistent with the D-10 "ships in release notes" framing).
- Source of the REST delta: F2's renamed routes + F1's holder routes + F4's composition route + F3's reservation API.
- Source of the Go-importer delta: D-1 rename inventory (`pkg/mmodel/alias.go` types) + D-11 path bump.

**Change shape.** Author one new markdown artifact, two tables. REST table: old route → new route (e.g. `/v1/holders/:holder_id/aliases*` → `/v1/holders/:holder_id/instruments*`), the `entity` error-body value change (`"Alias"`→`"Instrument"`), new holder ownership fields/reads, the tracer reservation endpoints. Go-importer table: each renamed `mmodel` identifier + the import-path bump `github.com/LerianStudio/midaz/v3/pkg/...` → `/v4`.

**Verification.**
```
test -f docs/v4/MIGRATION-v3-to-v4.md && echo EXISTS    # (or the settled location)
grep -ci 'instrument' <migration-guide>                  # REST + Go tables present
grep -ci '/v4' <migration-guide>                          # path-bump row present
```

**Dependencies.** F1–F5 merged (the migration delta is final only after the rename/routes/reservation API land). Inter-phase: X6 (external Go importers) consumes the Go table; X4/X5 (APIDog/docs portal) consume the REST table.

**Notes.** Mitigates R43 (error-body `entity` flip documented) and R46 (external Go importers). Gate 9 of §10. Q14-RESOLVED: external repos migrate on their owners' timing; the guide is informational, no coordinated cut.

---

### F6-T14 — Extend build.yml to upload tracer migrations to S3

**Objective.** Add a tracer-migrations S3-upload job to `build.yml`, additive alongside the existing ledger onboarding/transaction upload steps, leaving the ledger upload path untouched (Q15-RESOLVED, overrides the P5 tracer exclusion).

**Touchpoints (HEAD, verified).**
- `.github/workflows/build.yml` upload steps: `upload-onboarding-migrations` (`file_pattern: components/ledger/migrations/onboarding/*.sql`, `s3_prefix: ledger/onboarding/postgresql`) and `upload-transaction-migrations` (`components/ledger/migrations/transaction/*.sql`, `ledger/transaction/postgresql`). Both use `s3-upload.yml@v1.27.5`, bucket `lerian-migration-files`, `AWS_MIGRATIONS_ROLE_ARN`.
- Tracer migrations exist: `components/tracer/migrations/*.sql` (verified — `000001`..`000017+` up/down + `seeds/`).
- The build.yml comment block currently states "tracer migrations applied exclusively via tracer's bootstrap migration runner" and "crm/fees emit no image/migrations" — the new job ADDS the S3 publish without changing the applied-by path.

**Change shape.** Add one `upload-tracer-migrations` job mirroring the two ledger jobs: `file_pattern: components/tracer/migrations/*.sql`, `s3_prefix: tracer/postgresql` (key layout VERIFY against the managed-deploy pipeline at execution time), `strip_prefix: components/tracer/migrations`, `needs: [build]`, same bucket + role-arn secret. Additive only — the ledger jobs are unchanged.

**Verification.**
```
grep -n 'upload-tracer-migrations\|components/tracer/migrations' .github/workflows/build.yml   # new job present
# YAML well-formed:
python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/build.yml'))" && echo "YAML-OK"
```

**Dependencies.** None (independent additive CI change). Inter-phase: none.

**Notes.** Mitigates R45. Q15-RESOLVED. Gate 11-adjacent (fan-out sanity also reviews this step). DC: the S3 key layout must be verified against the managed-deploy pipeline at execution time — a wrong layout ships migrations nobody consumes. The ledger upload path stays untouched (additive change only).

---

### F6-T15 — PD-5 double-entry sum-zero re-proof under /v4

**Objective.** Re-run the standing double-entry sum-zero proof under the `/v4` module to confirm the mechanical bump did not perturb balance math.

**Touchpoints (HEAD, verified).**
- `pkg/mtransaction/validations_test.go` (sum-zero invariant suite).
- Transaction-side double-entry suites: `components/ledger/internal/adapters/http/in/transaction_*_test.go`.

**Change shape.** No edit — a re-run gate. F6 owns no balance-math change; this proves the path rewrite is inert.

**Verification.**
```
go test ./pkg/mtransaction/... -run 'Validation|SumZero|Balance'    # narrowest first
make test-unit                                                       # includes validations_test.go under /v4
```

**Dependencies.** F6-T02 (under /v4). Inter-phase: F3 owns the live double-entry untouched gate; F6 re-proves it under the bump.

**Notes.** Standing third rail (double-entry correctness, PD-5). Gate 6 of §10. A re-run, not a rewrite.

---

### F6-T16 — F3 usage-drift crash re-proof under /v4

**Objective.** Re-run the F3 usage-drift crash-convergence suite (and the CHAOS multitenant pair) under the `/v4` module, confirming the two-phase reservation counters still converge.

**Touchpoints (HEAD, verified).**
- `components/tracer/tests/integration/05_limits_verification_test.go`, `08_limits_time_window_test.go`, `12_audit_rollback_limit_test.go`.
- CHAOS pair: `16_multitenant_chaos_tm_outage_test.go`, `17_multitenant_chaos_redis_outage_test.go`. (DRIFT NOTE: the `17_*` glob ALSO matches `17_audit_actor_test.go`, which is NOT a chaos test — verified present at HEAD; name the two chaos files explicitly, do not glob `17_*`.)

**Change shape.** No edit — a re-run gate under /v4. The F3 reservation seam and tables must already be merged (F3 is a dependency of F6).

**Verification.**
```
go test -tags integration ./components/tracer/tests/integration/ -run 'Limits|Reservation|AuditRollback'   # narrowest
make test-integration   # over components/tracer/tests/integration (incl. 05/08/12)
CHAOS=1 go test -tags 'integration chaos' ./components/tracer/tests/integration/ -run 'MultitenantChaos'    # 16/17 pair
```

**Dependencies.** F6-T02 (under /v4). Inter-phase: F3 (the reservation model must be merged).

**Notes.** Mitigates R3, R4 (the F3 correctness invariants re-proved under the bump). Gate 7 of §10. DRIFT: the explicit two-file naming avoids the `17_audit_actor_test.go` false inclusion.

---

### F6-T17 — Full matrix green via root `make ci`

**Objective.** Run the root `make ci` (F0's deliverable) end-to-end under /v4 — unit (`-race`, untagged) → integration (`-tags=integration -p 1`) → tag-gated suites (`chaos`/`property`/`e2e`) incl. `./tests/reporter` → tracer BDD — and confirm one green verdict, including the reporter suites the charter requires.

**Touchpoints (HEAD, verified).**
- `make ci` does NOT exist at HEAD (`grep -nE "^ci:" Makefile mk/*.mk` → NONE) — F0-T05 builds it.
- Existing matrix pieces: `mk/tests.mk:105` (`test-unit`), `:224` (`test-integration`), `:293` (`test-bdd`), `:319` (`coverage-integration`). Integration discovery `find ./components ./pkg` at `:234`/`:329` (excludes `./tests` at HEAD — F0-T04 widens it).
- Reporter suites under `tests/reporter/integration/` — unreachable by any HEAD target; F0-T04/T05 make them reachable.

**Change shape.** No code change in F6 — a re-run gate. F6 CONSUMES `make ci`; it does not author it (F0 owns the harness). If `make ci` is absent at execution time, that is an F0 defect to route back, not new F6 scope.

**Verification.**
```
grep -nE "^ci:" Makefile mk/*.mk     # the target exists (F0 deliverable)
make ci; echo "ci exit: $?"           # one verdict under /v4
make -n ci 2>&1 | grep -iE 'reporter' # confirms ./tests/reporter is in the matrix
```

**Dependencies.** F6-T02..F6-T11 (the full /v4 + swept tree). Inter-phase: F0-T04/T05 (the `make ci` harness), F1–F5 (all behaviour merged).

**Notes.** Mitigates R25. Q16-RESOLVED. Gates 8 + 13 of §10 (8 = full matrix green; 13 = reporter reachable + green). One command, one verdict.

---

### F6-T18 — Release-version alignment (BREAKING CHANGE + semantic-release dry-run)

**Objective.** Ensure the release-trigger commit carries a `BREAKING CHANGE` footer (or the `!` marker) so semantic-release computes `4.0.0`, NOT a `3.x` minor — the module bump alone does not cut the tag.

**Touchpoints (HEAD, verified).**
- `.releaserc.yml:8-28` — `releaseRules` map `feat`/`perf`/`build`/`refactor` → minor, `fix`/`chore`/`ci`/`test`/`docs` → patch, and `breaking: true` → major. No `tagFormat` key (defaults to `v${version}`); branches `main`/`develop`/`release-candidate` at `:55-58`.
- **DRIFT (binding):** latest tag is `v3.8.0-beta.9` (NOT `3.7.x`, NOT a stable `3.x`). semantic-release's last-tag input on the release branch is a `3.8.0` prerelease. A `BREAKING CHANGE` from a `3.8.0-beta` base must be verified to compute `4.0.0` (not `3.8.0` stable) — the dry-run is the authority, not the plan's "verify latest tag is 3.x" assumption.

**Change shape.** No source rewrite — a release-engineering step. The trigger commit (the one that lands the v4 cut on the release branch) MUST carry `BREAKING CHANGE:` in the footer or use `feat!`/`refactor!`. Run the semantic-release dry-run and confirm the computed next version is `4.0.0`.

**Verification.**
```
git tag --sort=-v:refname | head -3                  # confirm the actual last tag (v3.8.0-beta.N at HEAD)
npx semantic-release --dry-run 2>&1 | grep -iE 'next release version|4.0.0'   # MUST show 4.0.0
grep -nE 'tagFormat|breaking' .releaserc.yml          # confirms no tagFormat; breaking→major rule present
```

**Dependencies.** F6-T02..F6-T17 (everything merged + green before the release commit). Inter-phase: none (terminal).

**Notes.** Mitigates R5 (Critical). Gate 10 of §10. The DRIFT (`v3.8.0-beta.9` base) makes the dry-run mandatory — do not rely on the plan's `3.x`-stable assumption. Record the dry-run output in `F6-EXECUTION-NOTE.md`.

---

### F6-T19 — Fan-out sanity (4 images, no crm/fees keys)

**Objective.** Confirm via dry-run / log review that `build.yml` builds exactly 4 images and the helm/gitops key mappings carry no crm/plugin-fees keys.

**Touchpoints (HEAD, verified).**
- `.github/workflows/build.yml:21-24` — `filter_paths` = ledger/tracer/reporter-manager/reporter-worker (4 components).
- `:29` `path_level: '2'`, `:30` `app_name_prefix: "midaz"`, `:37` `helm_chart: "midaz"`.
- `:44` `helm_values_key_mappings` = `{"midaz-ledger":"ledger","midaz-tracer":"tracer","midaz-reporter-manager":"manager","midaz-reporter-worker":"worker"}` (4 keys, no crm/fees).
- `:68` `yaml_key_mappings` = the `.tag`-suffixed distinct schema for the same 4 images (no crm/fees).
- `shared_paths` (`go.mod`/`go.sum`/`pkg/`/`Makefile`) rebuild ALL 4 — relevant since F6-T02 touches `go.mod` + `pkg/`.

**Change shape.** No edit — a review gate. Confirm the mappings are already crm/fees-free at HEAD (they are, verified) and survived F6's changes; confirm the F6-T14 tracer-migrations job did not perturb the image fan-out.

**Verification.**
```
grep -n 'helm_values_key_mappings\|yaml_key_mappings' .github/workflows/build.yml   # 4 keys each, no crm/fees
grep -c 'midaz-' .github/workflows/build.yml | head     # the 4 image names
grep -n 'crm\|plugin-fees' .github/workflows/build.yml || echo "NO-CRM-FEES-KEYS"
```

**Dependencies.** F6-T14 (the migration-upload addition must not change the fan-out). Inter-phase: none.

**Notes.** Mitigates R6 (Critical). Gate 11 of §10. The mappings are already correct at HEAD (P8/P9 left them crm/fees-free); this gate confirms nothing regressed.

---

### F6-T20 — DEPLOY-GATE: external lockstep sign-off before production tag

**Objective.** Record the external lockstep sign-offs that MUST land before any production v4 tag fans out: X1 (auth-server RBAC policy migration, Fred-owned), X2 (Helm `midaz` chart), X3 (gitops `midaz-firmino-gitops`), X4 (APIDog scenarios). The armed fallback is gate-the-production-tag (P8-T22).

**Touchpoints (HEAD, verified).**
- `build.yml:39-44` (helm lockstep comment + mappings), `:64-68` (gitops lockstep comment + mappings) — both flagged DEPLOY-GATED (P8-T20, R12).
- The external register lives in `docs/v4/PLAN.md` §13 (X1–X6) and the F0 inventory artifact (`docs/v4/F0-consumer-coordination-inventory.md`).

**Change shape.** No code change — a sign-off record in `F6-EXECUTION-NOTE.md`: each external owner (X1 Fred+plugin-auth, X2/X3 Ops, X4 QA/API) confirms before the production tag. X5 (docs portal) and X6 (Go importers) are informational, not blocking (per Q14). NO auth-enabled environment deploys v4 before X1 confirms.

**Verification.**
```
grep -n 'DEPLOY-GATED' .github/workflows/build.yml      # the lockstep gates are armed
# Sign-off recorded (manual gate — the artifact carries the four acknowledgements):
grep -ciE 'X1|X2|X3|X4|sign-off|lockstep' docs/v4/plan/F6-EXECUTION-NOTE.md
```

**Dependencies.** F6-T18 (version aligned), F6-T19 (fan-out sane). Inter-phase: X1 (F2 triggers, F6 gates), X2/X3 (Ops), X4 (QA/API).

**Notes.** Mitigates R1 (X1 auth policies) and R6 (X2/X3 helm-gitops fan-out). Gate 12 of §10. The lockstep is a register, not a repo deliverable (DC: "External lockstep is a register, not a repo deliverable"). Cutting a real tag before sign-off fans 4 images into a chart still keying crm/fees → ArgoCD sync fails (R6).

---

## 3. Gate-closure map

Every exit gate of §10, mapped to the task(s) that close it.

| §10 Exit gate | Closing task(s) |
|---------------|-----------------|
| Gate 1 — Module builds as /v4; zero `midaz/v3` in `.go` (excl. `.bin`/`coverage.out`) | F6-T02 (rewrite), F6-T03 (build/vet); F6-T01 (count) |
| Gate 2 — go.mod is the only module-graph change; no `replace`/`retract` | F6-T03 |
| Gate 3 — Grep-zero sweep report (all curated tokens zero, with carve-outs) | F6-T12 (report); rewrites F6-T04, F6-T05, F6-T06, F6-T07, F6-T08, F6-T10, F6-T11 |
| Gate 4 — OTEL attribution corrected (six `/v4` + tracer monorepo path) | F6-T04 |
| Gate 5 — chaos.go references only existing make targets | F6-T09 |
| Gate 6 — PD-5 double-entry re-proof green under /v4 | F6-T15 |
| Gate 7 — F3 usage-drift re-proof green (incl. CHAOS 16/17 pair) | F6-T16 |
| Gate 8 — Full matrix green via `make ci` | F6-T17 |
| Gate 9 — Migration guide reviewed (REST + Go tables) | F6-T13 |
| Gate 10 — Release-version alignment; semantic-release dry-run = `4.0.0` | F6-T18 |
| Gate 11 — Fan-out sanity (4 images, no crm/fees keys) | F6-T19; F6-T14 (tracer S3 additive, fan-out unperturbed) |
| Gate 12 — DEPLOY-GATE external lockstep sign-off before production tag | F6-T20 |
| Gate 13 — Reporter re-proof reachable + green (consumes `make ci`) | F6-T17 |

No gate is unmapped. (Gate 3 is the multi-token sweep: F6-T12 is the report task, but the underlying rewrites that make each token zero are spread across F6-T04/05/06/07/08/10/11 — listed so each token's closing edit is traceable.)

---

## 4. Risk-to-task map

Every `docs/v4/PLAN.md` §11 risk whose owning-phase column names F6, mapped to its carrying task(s).

| §11 Risk | Owning-phase note | Carrying task(s) |
|----------|-------------------|------------------|
| R5 (Critical) — module-bump / release-tag decoupling; semantic-release reads commit type + last tag, not go.mod | F0 (record), F6 (enforce) | F6-T18 (BREAKING CHANGE + dry-run = 4.0.0); F6-T01 (re-measure) |
| R6 (Critical) — production fan-out deploy-gated on external Helm/gitops repos | F0 (register), F6 (gate) | F6-T19 (fan-out sanity), F6-T20 (lockstep sign-off) |
| R1 (Critical) — `plugin-crm` namespace orphans RBAC grants; X1 release gate | F0 (register), F2 (flip), F6 (X1 gate) | F6-T20 (X1 sign-off before production tag); F6-T12 (namespace residue grep-zero) |
| R25 (High) — `tests/reporter` unreachable; "full matrix green" would skip reporter | F0 (harness), F6 (proof) | F6-T17 (consumes `make ci`) |
| R26 (High) — chaos.go calls non-existent `up-backend`/`down-backend` | F0 (fix), F6 (confirm) | F6-T09 |
| R27 (High) — OTEL_LIBRARY_NAME `/v3` invisible to Go sweep; tracer value already wrong | F0 (record), F6 (fix) | F6-T04 |
| R28 (High) — mechanical rewrite tool slip (catch `Account.Alias`, rewrite CHANGELOG history) | F6 | F6-T02 (path-only rewrite), F6-T03 (go.mod-only audit), F6-T12 (curated grep-zero w/ DO-NOT-TOUCH) |
| R30 (High) — Gate 3's "zero v3.7.0 outside CHANGELOG" trips on `docs/monorepo/analysis/03-midaz-host.md:145` | F6 | F6-T12 (exclude analysis tree OR rephrase in F6-T11 — pick one; gate and scope must not contradict) |
| R45 (Medium) — tracer S3 migration upload touches the deploy pipeline key layout | F6 | F6-T14 (additive job, ledger path untouched, key layout verified at execution time) |
| R46 (Medium) — external Go importers break on /v4; unverifiable in-repo | F6 | F6-T13 (Go-importer migration table); F6-T20 (X6 informational) |
| R49 (Low) — `GOVERNANCE.md:46` malformed `/v3` GitHub link | F6 | F6-T10 |
| R50 (Low) — `.bin`/`coverage.out`/postman backups carry `/v3`, pollute greps | F6 | F6-T07 (backups deleted/excluded), F6-T12 (grep gates exclude `.bin`/`coverage.out`) |

No F6-owned risk is unmapped. R31 (F5-owned phantom-holder-refs premise) is touched defensively by F6-T06 but its fix is F5's. R43 (the `entity` error-body flip) is F2-owned but its DOC obligation lands in F6-T13's migration guide. Phase-local §10-R8 (version-source re-inject) maps to F6-T05/F6-T06 (the source bump precedes regeneration so `generate-docs` cannot re-inject `v3.7.0`).

---

## 5. Execution-notes convention

`F6-EXECUTION-NOTE.md` is created DURING execution (in this directory), capturing decisions and commit SHAs as each task lands — mirroring `docs/monorepo/plan/P2a-EXECUTION-NOTE.md`. It is the live record of: the re-measured execution-time counts (F6-T01, superseding all HEAD baselines here); the mechanical-commit SHA and the sweep-commit SHA (the two-commit split); the semantic-release dry-run output proving `4.0.0` from the actual last tag (DRIFT: `v3.8.0-beta.9` at planning HEAD, not `3.x`-stable); the tracer-version DRIFT (4 hardcoded `0.1.0` literals incl. `docs.go:3124`, NOT templated-only as §10 claimed); the `plugin-crm` vs `plugin-crm-mongodb` distinction (container name is not namespace residue); the F6-T12 grep-zero report with its exact exclusion set and DO-NOT-TOUCH allowlist; the chosen resolution of the `docs/monorepo/analysis/` `v3.7.0` contradiction (exclude vs rephrase); the F6-T14 S3 key-layout verified against the managed-deploy pipeline; and the four X1–X4 lockstep sign-offs gating the production tag.
